# Archive — completed build specs

These docs described features that are now **built and shipped**. They're kept for reference
(markup shapes, class names, generator sketches) but are **not open tasks**. The live repo is the
source of truth; details here may be slightly stale.

| Doc | Feature | Now lives in |
|---|---|---|
| `FEATURE-INDEXES.md` | Feature & treasure index pages (folder cards, trait/ability previews, live search/filter) | `v2 → docs/stylesheets/steel-indexes.css`, `docs/javascripts/steel-feature-browser.js`, `steel-etl → internal/site/build.go` |
