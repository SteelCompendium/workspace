# Freeze tooling restore — 2026-09-06

Restored `check-freeze.sh` + `freeze-baseline.sha256` after the `.superpowers/` wipe, regenerated from a fresh clean clone of `origin/develop` @ `d8bda06` (SC-277; clone left at `.../scratchpad/freeze-restore/dse-clone`). Two clean `npm run shots` sweeps were byte-identical (508 PNGs/126 ids each); hashed the full frozen class (`*--steel-print`+`*--steel-realprint`) = **252 lines**, twin==realprint for all 126 ids, 0 exclusions. Cross-check vs SC-202's `sc202-r4-postedit-allshots.sha256`: all 252 pairs matched by hash, 0 mismatches; its 8 extra names (feature-list/title-nested/treasure-hr/perk-links ×2) are expected SC-202-only extras, not misses — gate cleared, baseline written.

Proofs (all as contracted): (a) clean clone's shots dir → `freeze OK (252/252 frozen print PNGs byte-identical — steel-print twin + steel-realprint since SC-170)`, exit 0. (b) SC-202 worktree shots dir (read-only) → same `freeze OK (252/252 ...)`, exit 0. (c) 1 byte appended to `ancestry--steel-print.png` in a scratch copy → `FREEZE VIOLATED (1 checksum mismatches, 0 missing)` / `ancestry--steel-print.png`, exit 1. (d) that file deleted instead → `freeze OK (251/252 producible OK, 1 missing (not producible on this branch), 0 checksum mismatches)`, exit 0.
