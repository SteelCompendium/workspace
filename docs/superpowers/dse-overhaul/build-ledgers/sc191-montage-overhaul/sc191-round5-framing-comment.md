Round 5 is running: equal-width tracks, the `Record…` rename, the "+" column gone, and your cheat-sheet idea mocked — which I think is right, not clunky, in a compact form.

**Your "+" column question, answered now:** that is the add-a-round ghost lane from round 2 — a click target for appending a round 4 to the montage. It does not earn its width: the ⋯ overflow menu already carries "add a round", the lane is invisible-until-hovered on most of its height (a discoverability failure), and it costs a full empty column on every card for an action used at most once or twice per montage. Round 5 removes it; "add a round" lives in the ⋯ menu alone. Say the word if you want it back.

**On the cheat-sheet — my position:** your instinct is right, and the reason it is not clunky is that the two panels answer different questions at different tempos. The foot panel answers "how does a montage work?" — read once, then closed. The tier table answers "is this 13 a success?" — asked on every single roll. One panel cannot serve both tempos, which is exactly the itch you are feeling. Two refinements to your sketch:

1. **Compact, not the full table.** The stylized strip above the board should be one row per difficulty (Easy / Medium / Hard) with the three roll bands (≤11 · 12–16 · 17+) as glyph cells — the same circled green check / circled red X seal language the board already speaks, with "c" marks for with-consequence tiers. A Director glances, finds their row, reads the band. The full four-row book table (nat 19–20 included) stays in the foot panel, so nothing is lost and nothing is stated twice at full size.
2. **Persistent once opened.** The strip should stay open until closed (a pin, not a peek) — "seen all the time by a lot of Directors" means the open state is the working state, and the card simply remembers it.

Round 5 will show the strip open and closed, plus where its toggle lives (my lean: a small button in the board's header row, since the strip belongs to the board, not the card foot). I am also having the sheet's Result field carry a one-line tier hint, since the sheet is where a specific number actually gets adjudicated — you can veto either piece on sight.

**The rename:** agreed, "Record…" is doing verb-noun double duty and reads like audio. Round 5 picks a replacement and shows it in place — leading candidates are "Log a test…" / "Add a test…"; the worker will argue for one.

Everything else from your comment is locked in: `merged` is the outcome band, and the two tracks become the same horizontal width — either limit ending the montage makes them the same scale, so the failure tiles widen to match. Shots when the round lands.
