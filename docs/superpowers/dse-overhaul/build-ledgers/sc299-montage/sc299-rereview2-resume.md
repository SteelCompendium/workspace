SC-299 scoped re-review 2: round 3 has landed on the same worktree. Review ONLY the round-3 delta.

- Worktree: /home/scott/code/steelCompendium/worktrees/sc299-montage/draw-steel-elements, now rebased onto origin/develop 5a5ed49 (SC-126 parity harness); head b2e40d1; superproject f229603 on origin/main 8685b65.
- Round-3 report (executive summary first): /home/scott/code/steelCompendium/workspace/.superpowers/sdd/sc299-montage/sc299-round3-report.md
- Delta: `git diff 6e5f35c b2e40d1` — the report names the replayed head; spot-check the replay by --stat as you did last time.
- Verify with file:line and MEASUREMENT (real Chromium under pointer: coarse, as in your HIGH-1 reproduction): HIGH-1 — both parts (containment + `--dse-mt-colmin` under coarse) at 560/700/900 px with 3, 4, 5 and 8 rounds; `elementFromPoint` 6 px inside a recorded cell's right edge returns that cell in every case; the new shoot.mjs in-run gate actually matches `(pointer: coarse)` and CAN FAIL (inject a regression and confirm it fails loudly, then revert). LOW-1 (no aria-label on the role-less cell; AX tree clean), LOW-2 (`lightValue('chip-bg')` or FILL_INK).
- Re-run: tsc, lint, jest (rm -f main.js styles.css first), shots (the new gate lives there — read its OK line), freeze, parity LAST (SC-126's new background-color comparator must still report 0 GAPs / 0 undeclared / 16 DECLARED). Logs → /home/scott/code/steelCompendium/workspace/.superpowers/sdd/sc299-montage/sc299-rv3-<gate>.log, gate command LAST in `bash -c`, foreground only.
- Report: /home/scott/code/steelCompendium/workspace/.superpowers/sdd/sc299-montage/sc299-rereview2-report.md — ≤10-line executive summary (verdict LAND-READY / FIX ROUND NEEDED, per-finding status, gate lines verbatim), then NEW findings by severity. Never call Linear; never commit; delete probe files; never edit freeze-baseline.sha256. Final text = raw facts + report path.
