SC-299 scoped re-review 3: round 4 (harness gate hardening + one comment) has landed. Review ONLY that delta — `git diff b2e40d1 d124cc3` (dse head d124cc3, ws bf4f2c3).
- Round-4 report: /home/scott/code/steelCompendium/workspace/.superpowers/sdd/sc299-montage/sc299-round4-report.md (executive summary + its can-fail log sc299-r4-canfail.log).
- Verify: MED-1 (viewport 2400; a null hit prints as its own OFF-SCREEN line and still fails; re-run YOUR colmin-only injection and confirm no `hit null` WRONG-WRITE line appears), LOW-1 (comment now states the measured two-row truth), INFO-1 (union of `.dse-mt__quick` boxes measured), INFO-2 (doc line). Confirm the delta touches only shoot.mjs and the one comment, no CSS declaration changed (`git diff b2e40d1 d124cc3 -- styles-source.css` must be comment-only).
- Re-run: tsc, lint, jest, shots (read the gate's OK line), freeze (260/260), parity LAST (0/0/16). Logs → …/sc299-montage/sc299-rv4-<gate>.log, foreground, gate LAST in `bash -c`.
- Report: …/sc299-montage/sc299-rereview3-report.md, ≤10-line executive summary (LAND-READY / FIX ROUND NEEDED, gate lines verbatim). Never call Linear; never commit; delete probes; tree clean after. Final text = raw facts + report path.
