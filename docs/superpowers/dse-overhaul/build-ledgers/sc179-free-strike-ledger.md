# SC-179 — decisions ledger

Effort: `sc179-free-strike` (worktree `/home/scott/code/steelCompendium/worktrees/sc179-free-strike`).
Owner: Fable ticket-owner, session 7e7c98aa-ea62-4a33-b1ef-e4274d58fe58 (2026-09-06).

## Rulings (verbatim, dated)

- **2026-08-29 (Scott, comment on the "Fixed and verified by rendering" report):**
  > approved

  Interpretation: the fix as described in the 2026-08-23 report (Free Strike Abilities grid on
  the rule page + common/universal-action search boost tier) is accepted as-is; proceed to land.
  The report's "one deferred half" (feature-group rules prose has no Browse page) was offered as
  a follow-up — Scott did not object, so it is filed as a Backlog ticket, not folded in.

## Owner decisions

- **2026-09-06:** Branch predates the 2026-08-27 Linear migration; its superproject commit
  `db07312` adds FOLLOWUPS #79 to a now-retired `FOLLOWUPS.md`. Rebase drops that commit and
  the follow-up is filed as a Linear Backlog ticket instead (see below).
- **2026-09-06:** steel-etl `161e8a1` is 15 commits behind `origin/main` (`c84b2e7`);
  superproject is 121 behind `db68bf1`. A rebase + full regen/verify round runs before
  land-ready.

## Follow-up tickets filed

- **SC-303** (Backlog, 2026-09-06): "Browse: feature-group rules prose (e.g. Ch. 10 'Free Strikes') has no Browse page" — the deferred half from the 2026-08-23 report; replaces the branch's FOLLOWUPS #79 commit `db07312`, which is dropped at rebase.

## Round log
- **Round 2 (2026-09-06, implementer):** rebased steel-etl → `f34c0eb` on `c84b2e7`; superproject → `188f11c` on `db68bf1` (FOLLOWUPS commit dropped). Gates all 0; leak check 1; boost tier 24 pages (round 1 said 23 — reviewer to identify the 24th). Report: `sc179-round2-report.md`.
- **Round 3 (dispatched 2026-09-06, reviewer):** independent review of the rebased delta — brief `sc179-round3-review-brief.md`.
- **Round 3 result (2026-09-06, reviewer):** VERDICT LAND-READY; HIGH 0 · MED 2 · LOW 4 · INFO 6. Boost tier is exactly 24 registry codes (round 1's "23" was a miscount). Idempotent, leak 1, blast radius 24 files. Report: `sc179-round3-review.md`.

## Owner rulings on round-3 findings (2026-09-06)
- **MED-1, MED-2, LOW-1, LOW-2, LOW-3, LOW-4, INFO-1 → fold into fix round 4** (all small, all in `action_abilities.go` / `build_test.go` / `search_boost.go` comment / `site-builder.md`). Rationale: Scott's "do the right thing over minimizing work"; every one has a prescribed fix and a fixture already written.
- **INFO-2 (prefix over-match on `feature.common.*`) → drop**: by design; all three buckets are universal actions. Fix round adds a one-line comment noting it.
- **INFO-3 (second search doc for the same page) → drop**: benign / useful.
- **INFO-4, INFO-6 → no action.**
- **INFO-5 (restore form misses `v2/docs/pins.md` + `steelCompendium.github.io/docs/api/v1/{index,scc}.json`) → report to dispatcher** for `.claude/orchestrate/PROJECT.md` §8.5; not a branch change.
- **Round 4 (fix):** resume the round-2 implementer (did not author the round-1 code, has the branch loaded). **Round 5 (scoped re-review):** resume the round-3 reviewer (did not author the fixes).
- **Round 4 result (2026-09-06, implementer):** all folded findings applied; steel-etl `c1a21db` (amended), superproject `ada80ce`. Gates 0; focused site tests 84 PASS; MED-1 guard proven (fails when the pass is moved before `embedItemCards`). Leak 1, boost 24. Report: `sc179-round4-report.md`. Follow-up noted, not actioned: pre-existing gofmt drift on 12 unrelated files on steel-etl `origin/main` → **drop** (not this branch's; a formatting-only sweep is not worth a ticket).
- **Round 5 (dispatched 2026-09-06, reviewer):** scoped re-review of the round-4 delta — brief `sc179-round5-rereview-brief.md`.
- **Round 5 result (2026-09-06, reviewer):** LAND-READY; MED-1/LOW-1/LOW-3/LOW-4/INFO-1/INFO-2 FIXED; **MED-2 PARTIAL** (book-scoped, not family-scoped: a same-book class ability with `subtype: free-strike` still joins the grid) and **LOW-2 PARTIAL** (annotation typo/rename half-match still silent). Round-4 delta changed no generated byte. Report: `sc179-round5-rereview.md`.
- **Owner ruling:** fold both PARTIALs into **round 6** (each ≈ a handful of lines with the reviewer's prescribed close); **round 7** = scoped recheck by the reviewer. Reviewer's side note that `internal/cli/site.go` prints `result.Errors` but exits 0 → **filed as a Backlog ticket** (deploy-gating semantics decision, beyond this branch).
- **Round 6 result (2026-09-06, implementer):** MED-2 + LOW-2 residuals closed; steel-etl `a9e6f06`, superproject `7fb5494`. Gates 0; 84 focused PASS; leak 1, cards 2, boost 24; real-corpus `site` error-free. Report: `sc179-round6-report.md`.
- **Round 7 (dispatched 2026-09-06, reviewer):** final scoped recheck — brief `sc179-round7-recheck-brief.md`.
- **Round 7 result (2026-09-06, reviewer):** LAND-READY; MED-2 FIXED, LOW-2 FIXED, LOW-1/LOW-3 re-probed and hold; generated output byte-identical to rounds 3/5. Report: `sc179-round7-recheck.md`.
- **Owner rulings on round-7 INFOs:** (1) zero-match error keys on host-page presence rather than ability-tree presence → **drop**: unreachable today (`genericSections` = `[Browse]`), and the host page's presence is itself proof the book is in the section. (2) "add a row here" comment / `site-builder.md` "one map row" now implicitly conditional on `feature.ability.common` → **drop**: the filter and its test make the constraint explicit in code; not worth another amend + recheck.
- **2026-09-06: LAND-READY.** steel-etl `a9e6f06` on `c84b2e7`; superproject `7fb5494` on `db68bf1`. Landing is the dispatcher's / Scott's move (`land-stack`, from the main checkout). Ledger snapshot to `docs/superpowers/dse-overhaul/build-ledgers/` at landing.
- **Adapter footgun for the dispatcher (from round 3 INFO-5):** the §8.5 restore form misses `v2/docs/pins.md` and `steelCompendium.github.io/docs/api/v1/{index,scc}.json` (written by `gen --all`) — add to `.claude/orchestrate/PROJECT.md` §8.5.

## Landing (2026-09-06)

- Rebased again at landing onto steel-etl `15f931b` / workspace `19afe56`: steel-etl `feaf130`, superproject `53c2192`; build/vet/test -race all 0 (`r8-*`). Landed as workspace merge `3e06357`; deployed as v2 `d6c38b5`, org-site `2d74f88`, workspace `e4817d0`.

## Round 2 executive summary — `sc179-round2-report.md`


## Executive summary

- **STATUS: DONE**
- steel-etl rebased sha: `f34c0eb7750d44eee3e7e7b0271015ec1f346070` (single commit, onto `origin/main` = `c84b2e7`)
- superproject sha: `188f11cba3c2b38e00ef2fc8589c53ffda67623d` (onto `origin/main` = `db68bf1`; FOLLOWUPS commit `db07312` dropped)
- Gates: build=0, vet=0, test -race=0 (8/8 packages ok, 0 FAIL; the 8 SC-179 tests all pass)
- gen=0, site=0, mkdocs build=0 (244.65s, 0 WARNING lines)
- Leak check: **1** file carries "Free Strike Abilities" (expected: exactly one)
- Search boost: 24 common/universal-action pages at boost 3 (report's "~23" is hedged; matches)
- Screenshot: `/home/scott/code/steelCompendium/workspace/.superpowers/sdd/sc179/r2-after-free-strike-abilities.png`
- Superproject diffstat vs origin/main: **only** `CHANGELOG.md` + `steel-etl` (as required); worktree fully clean after revert.


## Round 3 executive summary — `sc179-round3-review.md`


## Executive summary

- **VERDICT: LAND-READY** — shipped output is correct on every axis I probed; no HIGH.
- Findings: **HIGH 0 · MED 2 · LOW 4 · INFO 5**. Neither MED changes today's output; both are
  latent-regression guards. Owner's call whether to fold them in or file as follow-ups.
- Gates (steel-etl `f34c0eb` on `origin/main` `c84b2e7`): build=0, vet=0, `test -race ./...`=0
  (8/8 pkgs ok), focused `-run 'Build|Order|Action|Boost' -v` = 81 PASS / 0 FAIL.
- Pipeline: gen=0, site=0, `mkdocs build --strict`=0 (218.25 s, **0 WARNING lines**).
- **Boosted pages: 24** (main 200 boost-3 pages → branch 224). **0 displaced.** The "24th" is
  not an over-match: the tier is exactly the 24 registry codes in the four intended
  type-paths; round 1's "23" was an arithmetic miscount (7+4+10+3=24), proven below.
- **Leak: 1** — `Browse/feature/common/main-actions/free-strike.md` is the only file carrying
  `Free Strike Abilities`; heading count on that page = 1.
- **Idempotency: PASS** — two consecutive `site` runs produce byte-identical trees (`diff -rq`
  over 3,091 files + 3,086 stubs = 0 differences).
- **Blast radius: 24 files**, all explainable (23 frontmatter-only boost, 1 boost + grid).

---


## Round 4 executive summary — `sc179-round4-report.md`


## Executive summary

- **STATUS: DONE**
- New steel-etl sha: `c1a21db8657dd63c93bafdb26f61385f1ad8dc87` (amended `f34c0eb`, same author/date, single commit)
- New superproject sha: `ada80ce` (amended `188f11c`, `steel-etl` gitlink bumped, message unchanged apart from the sha)
- Gates: build=0, vet=0, `test -race ./...`=0 (8/8 pkgs ok), `gofmt -l internal/` on every file this round touched = clean (pre-existing drift on 12 unrelated files, none touched by SC-179, left alone — see Follow-ups)
- Focused run `-run 'Build|Order|Action|Boost' -v`: **84 PASS / 0 FAIL** (round 3 saw 81)
- gen=0, site=0; leak check = **1** file, heading count = **1**, boosted pages = **24**
- MED-1 guard proof: moving the pass before `embedItemCards` makes the new test FAIL (2 files with the heading) — confirmed, then restored
- `git diff origin/main...sc179-free-strike --stat`: still only `CHANGELOG.md` + `steel-etl`
- Worktree clean: superproject, steel-etl, v2, steelCompendium.github.io all `git status --short` empty


## Round 5 executive summary — `sc179-round5-rereview.md`


## Executive summary

- **VERDICT: LAND-READY.** No regressions; generated output is bit-for-bit what round 3 verified.
- **MED-1 FIXED** · **MED-2 PARTIAL** · **LOW-1 FIXED** · **LOW-2 PARTIAL** · **LOW-3 FIXED** ·
  **LOW-4 FIXED** · **INFO-1 FIXED** · **INFO-2 FIXED** — 0 NOT FIXED, 0 REGRESSED.
- Both PARTIALs are *latent residuals with no effect on today's corpus*, not defects: MED-2
  filters cross-**book** but not same-book class abilities; LOW-2 errors on the host-missing
  half-match but the annotation-rename half stays silent. Neither blocks landing.
- Gates: build=0, vet=0, `go test -race ./...`=0 (8/8 pkgs ok), focused run **84 PASS / 0 FAIL**
  (round 3: 81), `gofmt -l` on the 4 touched files clean.
- **MED-1 guard proof:** moving the pass before `embedItemCards` makes
  `TestBuild_ActionAbilityGridNotTranscluded` **FAIL** (2 files carry the heading) and it is the
  **only** failing test in the package. `build.go` restored byte-identical (md5 match).
- **LOW-2 corpus check:** `site` over the real corpus prints **no `Errors:` line at all** — the
  new error path is silent where it should be.
- gen=0, site=0; **leak = 1** file / heading count **1** / 2 cards; **boost = 24** pages.
- Idempotency: second `site` run byte-identical (`diff -rq` = 0). Blast radius vs `origin/main`
  build: still exactly **24** files, same set, 0 added/removed. No perf cost (6 alternating runs).

---


## Round 6 executive summary — `sc179-round6-report.md`


## Executive summary

- **STATUS: DONE**
- New steel-etl sha: `a9e6f06876c15591e0bbb8730ee749404f8a0fd7` (amended `c1a21db`, same author/date, single commit)
- New superproject sha: `7fb5494` (amended `ada80ce`, message unchanged apart from the sha)
- Gates: build=0, vet=0, `test -race ./...`=0 (8/8 pkgs ok), `gofmt -l` on all 4 touched files = clean
- Focused run `-run 'Build|Order|Action|Boost' -v`: **84 PASS / 0 FAIL** (unchanged count — this round extended existing tests rather than adding new top-level ones)
- gen=0, site=0; leak = **1** file, heading = **1**, boost = **24**, grid cards = **2**
- **Real-corpus `site` log: no `Errors:` line, no `action-abilities:` text** — confirmed clean
- `git diff origin/main...sc179-free-strike --stat`: still only `CHANGELOG.md` + `steel-etl`; FF-safe
- All four repos (`super`, `steel-etl`, `v2`, `steelCompendium.github.io`) `git status --short` empty


## Round 7 executive summary — `sc179-round7-recheck.md`


## Executive summary

- **VERDICT: LAND-READY.** Both round-5 PARTIALs are closed; no regression anywhere I probed.
- **MED-2 FIXED** — same-book class ability no longer renders; grid `cards=2`. Cross-book,
  scc-less, non-`feature.ability.common` bucket, and `feature.ability.common.sub` leaves are all
  excluded; the canonical pair survives both filters.
- **LOW-2 FIXED** — all three renamed/typo'd `@subtype` variants now error with an actionable
  message; abilities-without-host still errors; a section with neither side stays silent.
- Gates: build=0, vet=0, `go test -race ./...`=0 (8/8 pkgs ok, 0 FAIL), focused run
  **84 PASS / 0 FAIL**, `gofmt -l` on the 4 touched files clean.
- gen=0, site=0; real-corpus `site` log has **no `Errors:` and no `action-abilities:` text**.
- **leak = 1** file / heading = **1** / grid cards = **2**; **boost = 24** (224 boost-3 pages total).
- Generated output byte-identical to rounds 3 and 5: `v2` dirt = 501 files, +7217/−3893.
- Delta touches only `internal/site/action_abilities.go` + its test → no blast-radius rerun
  (the identical 501/+7217/−3893 is itself the confirmation).
- One **INFO** (not a finding, not land-blocking): the new zero-match error keys on host presence,
  not on ability-tree presence — see §3.

---


