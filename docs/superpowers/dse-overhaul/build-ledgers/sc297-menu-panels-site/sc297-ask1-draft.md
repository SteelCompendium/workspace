**Ask: pick the panel scope and answer the four questions below — the statblock and ability-card prototype is ready to look at.**

The floating button strip at the top-center of a card (copy link, pin, add to encounter, export) is replaced by the same plate the DSE plugin uses: a short icon-only tab seated on top of the card's top-right corner, outside the card, hidden until you hover or tab into the card. Same geometry and material as the plugin's panel (right edge 10px inside the card frame, bottom edge resting on the card's top border, hairline crown, tucked behind the card).

## What you're deciding

1. **Which cards get the panel.** Recommendation: TBD
2. **Collapse toggle.** The plugin panel's rightmost control folds the element to one line. Recommendation: TBD
3. **Plain-page strip (`.sc-pageact`, always-visible top-right on non-card pages).** Recommendation: TBD
4. **Cards embedded in Read chapters.** Recommendation: TBD

Approve = the prototype below rolls out per your answers. Change anything = say what, and it goes back for one round before rollout.

## The prototype

BEFORE — statblock, dark scheme, hovered (current floating strip):
{{IMG:TBD}}

AFTER — statblock, dark scheme, hovered (panel visible):
{{IMG:TBD}}

AFTER — statblock, light scheme, hovered:
{{IMG:TBD}}

AFTER — corner join, tight crop (the card's own border runs unbroken beneath the plate):
{{IMG:TBD}}

AFTER — phone width, panel always visible, nothing overlapped:
{{IMG:TBD}}

## For the record

- Worktree `sc297-menu-panels-site`; `v2` @ TBD. Prototype touches: TBD.
- Gates: unit TBD/TBD before → TBD/TBD after; e2e TBD.
- Full survey and port plan: `.superpowers/sdd/sc297-menu-panels-site/sc297-round1-port-spec.md` (machine-local).
