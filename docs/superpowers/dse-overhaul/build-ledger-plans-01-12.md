# SDD progress ledger — DSE framework build (worktree: dse-framework)

Plan 01 (test harness): /home/scott/code/steelCompendium/workspace/docs/superpowers/dse-overhaul/plans/2026-07-01-plan-01-test-harness.md
Worktree DSE: /home/scott/code/steelCompendium/worktrees/dse-framework/draw-steel-elements (branch dse-framework)

## Plan 01 — test harness (12 tasks)
(pending)

Task 1: complete (commit 14e6aca, review clean — Spec ✅ / quality Approved)
  Minor (for final review): (a) `.gitignore:10` bare `package-lock.json` line now stale (file tracked); (b) verify js-yaml major matches Obsidian's bundled 4.x.

Task 2: complete (commits ec5193d, 6e9ac40, review clean — Spec ✅ / quality Approved)
  Minor/WATCH (for byte-fidelity tasks T-3 round-trip + StaminaBar in D1): js-yaml dump() uses default lineWidth:80; may diverge from Obsidian's frontmatter formatting on long strings. If a round-trip byte-compat test fails, tune dump() (lineWidth:-1 etc.) to match Obsidian.
  Env note (confirmed): run node/npm via WORKSPACE devbox from workspace root: devbox run -- bash -c "cd <WT>/draw-steel-elements && <cmd>" (node v24). DSE-local devbox has no node.

Task 3: complete (commit 1123a3b, review clean — Spec ✅ / quality Approved; all assertions traced to source)

Task 4: complete (commit 597f755, review clean — Spec ✅ / quality Approved; test.failing verified desired-behavior, not traps)

Task 5: complete (commit 695bdce, review clean — Spec ✅ / quality Approved; CB-1/CB-2/DC-6 desired-behavior encodings verified against source + focused run)

Task 6: complete (commit a998726, review clean — Spec ✅ / quality Approved; death-floor + temp-first verified vs source)

Task 7: complete (commit 07ad3fb, review clean — Spec ✅ / quality Approved; CB-15 pin accurate, AJV validation real)

Task 8: complete (commit 0d4cc6a, review clean — Spec ✅ / quality Approved; 18 assertions traced to source incl. tricky lying-penalty + unmark-order quirk. Minor: report sub-block miscount, cosmetic)

Task 9: complete (commits ad09009 + fix 77dec72, review clean — Spec ✅ / quality Approved; Important finding [steps 1/2 not chain-order-isolated, plan-mandated] FIXED with decoy files, 71/71 pass)

Task 10: complete (commit ec6bc72, review clean — Spec ✅ / quality Approved; snapshots verified genuine SDK 2.1.5 output via compiled-source read; fixtures byte-match docs; deterministic. The F2 upgrade net is in place.)

Task 11: complete (commit 5e30240, review clean — Spec ✅ / quality Approved; one-write-per-click genuine exactly-once; selector deviation verified real-behavior. F1 element-test template established.)

Task 12: complete (commit f9135ce, review clean — Spec ✅ / quality Approved; CI continue-on-error correctly on tsc step only; mkdocs untouched)

=== PLAN 01 (test harness) COMPLETE — 12/12 tasks. Canonical suite: 14 suites / 111 tests / 3 snapshots, 0 fail. Branch dse-framework, b80a8a9..f9135ce ===
Minor findings roll-up for final whole-branch review:
  - .gitignore:10 still lists package-lock.json though it's now tracked (Task 1; harmless, cosmetic).
  - js-yaml dump() uses default lineWidth:80; may diverge from Obsidian's frontmatter formatting on long strings (Task 2 watch item; unverifiable from this repo; matters for future byte-fidelity round-trip tests / StaminaBar in D1).

=== PLAN 01 FINAL whole-branch review (opus): READY TO MERGE = YES ===
No Critical/Important. Minors: (1) .gitignore stale package-lock.json line — FIXING NOW; (2) js-yaml dump() lineWidth default — future (StaminaBar byte-fidelity, align dump options ~lineWidth:-1 before those tests); (3) test.failing could pass on a null-selector TypeError — mitigated by sibling plain tests + promote-on-fix; promote each test.failing to plain test() when its bug is fixed and confirm it then PASSES. Harness sound to build framework-core + migrations on.
Canonical: 14 suites / 111 tests (101 plain + 5 test.each rows + 5 test.failing) / 3 snapshots, 0 fail. Branch dse-framework @ f9135ce.

Final-review Minor #1 FIXED: commit da7094e (dropped stale .gitignore package-lock.json line; still tracked).
=== PLAN 01 fully complete & clean @ da7094e. Awaiting Scott's go for framework core (M-C) + landing decision. ===

################################################################
PLAN 02 — Framework core (10 tasks, lean plan, bottom-up order)
Plan: docs/superpowers/dse-overhaul/plans/2026-07-01-plan-02-framework-core.md
Same branch dse-framework; land foundation together later (Scott's call). Implementers sonnet (real prod code); reviewers sonnet; final review opus.
Order: 1 ValidationService · 2 SessionStore · 3 ReferenceService · 4 theme+prefs seams · 5 BlockHost/ReadingModeBlockHost · 6 RenderContext · 7 ElementView · 8 registry · 9 pipeline · 10 main.ts wiring + lint
################################################################

FW Task 1 (ValidationService): complete (commit 54ab6fc, review clean — Spec ✅ / quality Approved; plugin-scoping + compile-once cache + port fidelity all verified).
  Minor (for final FW review): (1) add source comment on addDependencySchema (fail-loud vs legacy swallow); (2) add source comment on validate re duplicate-$id-across-elementIds fail-fast (schema-authoring constraint, OD-4).
  Forward-note for FW Task 10 (main.ts wiring): wrap addDependencySchema calls so a malformed dependency schema degrades gracefully instead of crashing plugin onload.

FW Task 2 (SessionStore): complete (commit b58a6d9, review clean — Spec ✅ / quality Approved; isolation + purity verified).
  Minor (for final FW review): SessionStore added clear() (3-method superset of F1 §4.3's get/set), while ValidationService relies on drop-the-reference-on-unload. Settle a services-dispose convention (explicit dispose()/clear() vs ref-drop) at final review or Task 10 wiring; correct the report's "verbatim" wording.

FW Task 3 (ReferenceService + providers): complete (commit da3bd68, review Approved — Spec ✅; §3.7 verbatim, port fidelity + override order verified).
  *** CRITICAL migration constraint (carry into D1 / F1 steps 8-9): the initiative & negotiation element defs MUST use custom resolveRefs + autoResolveRefs:false, scoped to the statblock field — NOT the default deep-walk. Docs' user-facing statblock syntax is BARE paths ("Thorn Dragon"), which the new provider-chain no longer auto-resolves; the default path would silently break existing encounter blocks. ***
  Minor (final FW review): (1) refs.ts dropped legacy console.warn before file-not-found throw (harmless); (2) add a spy-test that FakeMetadataCache.getFirstLinkpathDest receives the real sourcePath (currently the mock ignores arg2, so the improvement is untested).

FW Task 4 (theme+prefs seams): complete (commit 7c7fba4, review Approved — Spec ✅; §3.5/§3.6 verbatim public surface; theme-descriptor-no-attr contract proven; setActive internal seam + Component.register mock fix both adjudicated correct/non-destructive, 154/154).
  Minor (final FW review): mock Component.unload() has no re-entrancy guard (pre-existing, not introduced) — harden if the mock gets more load-bearing.

FW Task 5 (BlockHost + ReadingModeBlockHost): complete (commit 9c35995, review Approved — Spec ✅; ATOMIC write path verified by hand-trace vs both CB-3 race + stale-position bug; fence/alias preserved [CB-5 fixed]; canvas quarantine; §3.4 verbatim; 163/163). Load-bearing write path is correct-by-construction.
  Minor (final FW review): (1) blockKey() no-block-info fallback uses ctx.docId not spec's "content-hash" (acceptable; content-hash unbuildable from §3.4 constructor sig; SessionStore best-effort only); (2) the "block moved/vanished under us" abort branch (ReadingModeBlockHost.ts:277-282) is untested; (3) redundant getSectionInfo call in replaceSource (stylistic).

FW Task 6 (RenderContext): complete (commits 5339bb9 + fix 194a2ff, review Approved — Spec ✅; §3.2 verbatim, mode===host.mode, roll?/RollService stub minimal, immutable via Object.freeze; unit count 111 verified real).
  Important FIXED: context.test.ts had introduced 4 new tsc errors (mock literals) — fixed by using real mock App/Plugin classes + `as any` (matches refs.test.ts convention); 2 tautological tests tightened.

FW Task 7 (ElementView base + HeroPanel stub): complete (commit 87b7a81, review Approved — Spec ✅; ML-1 fix proven [renderMarkdown owner===view], debounce coalescing + flush-on-unload proven via fake timers, serialize-injection/flush-via-register/fail-fast all adjudicated sound; registerDomEvent mock fix correct+non-destructive; 183/183, 0 new tsc). Harness mock now: Component.register + registerDomEvent both teardown on unload.
  Minor (final FW review): (1) report test-count miscount (13 not 14, cosmetic); (2) add JSDoc noting persist() throws SYNCHRONOUSLY (not rejects) if serializer unset.

FW Task 8 (ElementRegistry + ElementDefinition/ElementShape): complete (commit 51843d6, review Approved — Spec ✅; §3.1 field-for-field, all 3 rejections tested, get by id+aliases, import type only [no cycle], zero Plugin coupling, tsc unchanged; 197 total). Both minors (authoring?:unknown; within-tuple alias-dup) adjudicated non-blocking.

FW Task 9 (ElementPipeline + renderErrorCard): complete (commit f651240, review Approved — Spec ✅; §2.4 lifecycle order + all 3 ref-branches + serialize wiring end-to-end + single error boundary/no-half-mount all proven by property-pinning tests; renderErrorCard §3.8 verbatim; 217/217, 0 new tsc; scope clean — "mock fix" was local to the test). Integration keystone works.
  Minor (final FW review): (1) add a test for a REJECTING custom resolveRefs (reference stage); (2) add a spy-assertion that prefs.reflect() is called before mount (currently only observable via theme.apply); (3) schema-stage error path (validation itself throwing) untested — low risk.

FW Task 10 (main.ts wiring + import-boundary lint): complete (commit 2adcb1e, review Approved — Spec ✅; coexistence byte-identical [legacy registerElements untouched, new registry empty], addDependencySchema resilience proven through real onload, config-only lint rule [no eslint install, .eslintrc verified parseable], jest.config mapper fix non-destructive; 228/228, 0 new tsc). Minor: main.ts indent inconsistency (pre-existing); frameworkV2DependencySchemas() test-seam (adjudicated acceptable).

################################################################
=== PLAN 02 (Framework core) COMPLETE — 10/10 tasks, all reviews clean. ===
Branch dse-framework, framework-core increment da7094e..2adcb1e (12 commits).
228 tests / 24 suites / 3 snapshots, 0 fail, 0 regressions, 0 new tsc errors.
src/framework/: validation, session, seams/{refs,theme,prefs}, host/{BlockHost,ReadingModeBlockHost,LivePreviewBlockHost-stub}, context, view (+HeroPanel stub), registry, pipeline (+renderErrorCard). Wired into main.ts alongside legacy. import-boundary lint (config-only, inert until F3 installs eslint).
Accumulated Plan 02 Minors (for final review triage): T1 doc-comments (addDependencySchema fail-loud + validate dup-$id); T3 console.warn drop + sourcePath spy-test; T4 mock unload re-entrancy; T5 blockKey docId + abort-branch untested + redundant getSectionInfo; T7 persist() sync-throw JSDoc; T9 rejecting-resolveRefs test + prefs.reflect spy-test + schema-stage untested. Open convention: services-dispose (SessionStore.clear used in onunload; ValidationService relies on ref-drop).
### CRITICAL migration constraint still stands: initiative/negotiation migration MUST use custom resolveRefs + autoResolveRefs:false (bare-path statblock refs). ###
################################################################

=== PLAN 02 FINAL whole-increment review (opus): READY TO BUILD THE D-WAVE ON THIS = YES ===
Traced full persisted-block end-to-end; modules compose, no lifecycle gap; mode seam is a clean LP drop-in; CB-3/CB-5 fixes real+tested; coexistence airtight; interface names match F1 §3 verbatim; disposal convention coherent (SessionStore.clear in onunload + ref-drop); no runtime import cycles.
IMPORTANT findings (act before D-wave):
  I1 — autoResolveRefs default is ON: pipeline deep-walks ENTIRE raw YAML for refs unless def sets false. Legacy only initiative resolved refs (1 field, MERGE semantics). Default-ON = every migrated element gets whole-YAML ref-walk it never had + any bare scc:/@/[[]] value hits resolution (scc: THROWS until F2 -> error card). Blast radius narrow (whole-string matches only) but silent. => DECIDE: flip framework default to OFF/opt-in (rec) vs keep ON. One-line pipeline change + F1 amendment. Needs Scott before D-wave.
  I2 — initiative/negotiation migration MUST use custom resolveRefs + autoResolveRefs:false with FIELD-LEVEL MERGE (copy name/max_stamina/image, keep string) — not just bare-path, merge-not-replace. Capture where D-wave executor sees it.
MINOR (triage): M4 add pipeline error-branch tests (rejecting resolveRefs, throw-in-validate, block-vanished abort, prefs.reflect-before-mount spy) BEFORE first persisted migration (do while elementless). M1 blockKey docId collision (FOLLOWUP). M2 unload half-mounted view on error (1-line, ride w/ first element). M3 redundant getSectionInfo (ignore). T1/T4/T7/T10 doc/test niceties (ride). T3 = already a behavioral improvement (real sourcePath), only spy-test missing.

FW HARDENING (Scott-approved post-final-review): complete (commits a53de8e autoResolveRefs opt-in + 76b8cbc error-branch tests, review Approved — Spec ✅; flip correct [3-way branch, default SKIPS], 4 error-branch tests genuine [rejecting resolveRefs, throw-in-validate distinct path, invocationCallOrder ordering, block-vanished byte-identical], 233/233, 0 new tsc). F1 §2.4/§3.1 amended to default-OFF by orchestrator.
  Minor -> FIXING: registry.ts ElementDefinition.resolveRefs/autoResolveRefs JSDoc still says default-ON; 1-line fix to match spec.

=== Framework-core wiring seam (confirmed for D1): registry is PURE storage; main.ts initializeElementFrameworkV2 builds services+empty registry+pipeline but wires NO registerMarkdownCodeBlockProcessor. D1 Task 1 (HR) MUST add the "for each registered def+alias -> registerMarkdownCodeBlockProcessor(alias, (src,el,ctx) => framework.pipeline.run(def, src, new ReadingModeBlockHost(plugin,el,ctx,alias))) " loop + remove ds-hr from legacy RegisterElements. ===

registry.ts JSDoc fix: commit 0b9b164 (autoResolveRefs default-off doc; tsc still 11).
=== FRAMEWORK CORE fully complete + hardened + clean @ 0b9b164. Ready for D-wave. ===

################################################################
D-WAVE / PLAN D1 — Vue removal + first migrations (6 tasks)
Plan: docs/superpowers/dse-overhaul/plans/2026-07-02-plan-03-d1-vue-removal.md
Order: 1 HR migration + framework->Obsidian wiring loop · 2 Skills (interactive, seed kit) · 3 StaminaBar (persisted, byte-compat + dump() align) · 4 Vue teardown (gated on 1-3 green) · 5 tooling swap vue-tsc->tsc + fix 11 legacy tsc errors · 6 docs/ADR.
Implementers: sonnet (migrations = real prod code); reviewers sonnet.
KEY: Task 1 must add registerFrameworkElements(plugin, framework) loop (registry -> registerMarkdownCodeBlockProcessor -> pipeline.run) — missing after Plan 02.
################################################################

D1 Task 1 (HR migration + framework->Obsidian wiring loop): complete (commit 193d61c, review Approved — Spec ✅; wiring routes each alias to pipeline.run [reference-eq test], HR reuses legacy builder, de-registration clean [other 8 elements byte-unchanged, horizontalRuleProcessor.ts kept], HorizontalRule.vue deleted; 250/250, 0 new tsc, build ok). First real element on the framework.
  Minor (ride): .ds-hr-container CSS stays globally-scoped (shared w/ unmigrated statblock/featureblock via direct build() call) — re-scope under [data-dse-element] when statblock/featureblock migrate (F1 steps 5/6). Correct per D1 spec §3 "CSS: none" for HR.

D1 Task 2 (Skills migration + seed framework/kit/): complete (commit a300350, review Approved — Spec ✅; session-only collapse structurally unreachable to note-write, genuine remount test, behavior parity vs deleted Vue, Skills.ts/SkillsData.ts unchanged, kit widgets generic+tested, CSS reasoned both ways [ds-skill-* scoped, ds-kit-* left global for StaminaBar reuse]; 284/284, 0 new tsc). SkillList.vue+SkillGroup.vue deleted.
  Minor (for D2 kit work): kit/collapsible listener accumulation — mountComponentWrapper's renderContent re-invokes on each whole-element expand, registering a fresh registerDomEvent for the new heading indicator without unregistering the prior detached one (bounded by whole-element toggle count, cleaned on view unload). Tighten when D2 builds on the kit.

D1 Task 3 (Stamina Bar migration — persisted, LAST Vue element): complete (commit 27363e6, review Approved — Spec ✅; byte-compat by construction [traced vs CodeBlocks], persist via one replaceSource + fence/alias preserved, StaminaBar.ts unchanged, DOM StaminaEditModal [not Vue], kit componentWrapper reused, canPersist-false degrade impl+tested, CB-17 fixed as consequence; 308/308, 0 new tsc). StaminaBar.vue+StaminaAdjustor.vue+StaminaEditModal.vue deleted. *** ALL 3 VUE ELEMENTS MIGRATED — teardown (Task 4) unblocked. ***
  *** PROGRAM FINDING: Obsidian's stringifyYaml is the `yaml` npm package, NOT js-yaml (corrects F3 §4.2). Mock uses js-yaml as approximation — byte-identical for SCALAR DTOs (StaminaBar), but FREE-TEXT fields (Negotiation motivations, Initiative, Counter labels) will diverge in folding. FOLLOWUP before Negotiation/Initiative migration: either add real `yaml` as a test-dep (NEW DEP — needs Scott) or test free-text byte-compat differently. ***
  Minor (ride): modal-close-callback accumulation on repeated open/cancel (same class as Task 2 kit minor) — tighten in D2 kit work.

D1 Task 4 (Vue teardown): complete (commit 40eff8b, review Approved — Spec ✅; 0 .vue, 0 live vue refs, all 16 F3 SFCs accounted, 14 files deleted grep-verified, esbuild copyToStylesPlugin reworked [CSS pipeline verified, build ran], package-lock byte-identical, 308/308). *** VUE FULLY REMOVED — 2026-04-06 revert decision EXECUTED. Single rendering strategy. ***
  Minor (deferred to Task 5/6): build/tsc scripts still shell vue-tsc (undeclared; Task 5 swaps); @drawSteelComponents/* path alias now dead (optional cleanup).

D1 Task 5 (vue-tsc->tsc + fix legacy tsc errors): complete (commit 8b5088b, review Approved — Spec ✅; tsc 11->0 all TYPE-ONLY [verified line-by-line vs call sites], scripts+justfile swapped, dead @drawSteelComponents alias removed, 308/308, both builds ok). *** tsc now GREEN — real type-check gate possible. ***
  Loose ends -> FOLD INTO TASK 6: (1) prune package-lock.json (still lists vue pkgs; Task 4 avoided npm install); (2) flip plugin-ci.yml tsc step from continue-on-error to hard gate (F3 OD-6, now unblocked); CodeBlocks ctx.el cast = acceptable (proper el-threading deferred to that file's migration).

D1 Task 6 (finalization: lockfile prune + CI hard-gate + docs): complete (commits 9ef60c1 + 186b6f4, review Approved — Spec ✅; lockfile 669->561 pkgs zero version-churn/zero surprise-adds/all-vue-removed, CI tsc gate hard, docs verified vs every framework file; tsc 0, 308/308).

################################################################
=== PLAN D1 (Vue removal + first migrations) COMPLETE — 6/6 tasks, all reviews clean. ===
Branch dse-framework, D1 increment 0b9b164..186b6f4. HR + Skills + StaminaBar on Framework v2 (via registerFrameworkElements wiring); other 8 elements legacy; Vue FULLY GONE (deps+lockfile+SFCs+build); tsc GREEN (0); CI type-check now hard-gated; docs updated. 308 tests.
Accumulated D1 Minors (final-review triage): T1 HR CSS global until statblock/featureblock migrate; T2/T3 kit + modal listener accumulation (D2); T3 yaml-vs-js-yaml FREE-TEXT divergence (before Negotiation/Initiative migration — README open decision); T5 CodeBlocks ctx.el cast (until that file migrates); T6 conventions.md/project.md/index.md still Vue-stale (follow-up doc pass).
################################################################

=== PLAN D1 FINAL whole-increment review (opus): SOUND TO CONTINUE THE D-WAVE = YES ===
Pattern generalizes: Feature/Featureblock/Statblock = light pipeline-adoption (already DOM; use parse(data,raw)+opt-in resolveRefs); Counter = proven StaminaBar shape; Negotiation/Initiative = real stress test, pattern holds but need onUpdate targeted-update methods (framework supports).
GATING FIXES before Negotiation/Initiative migration (do as a one-time kit pass):
  (A) kit listener/modal accumulation: mountComponentWrapper owns a child Component for its content body, unload/replace per collapse<->expand cycle; scope modal-close registration so re-open replaces prior closer.
  (B) free-text YAML serializer decision: pick canonical serializer (Obsidian uses `yaml` pkg not js-yaml) + add a free-text round-trip golden test. StaminaBar byte-compat NOT undermined (scalar-only). [README open decision.]
  (C) style .dse-error-card (0 CSS today; now user-reachable in prod via the 2 schema'd migrated elements) — fold into the same pre-rollout kit pass.
RIDE: T1 HR CSS (re-scope when statblock/featureblock migrate), T5 ctx.el cast, T6 stale docs (conventions/project/index).
Process for remaining 8: keep per-element "registered exactly-once + end-to-end render" test as a checklist item; optionally 1 cross-check test (no alias in both legacy reg + framework registry).
Note: Skills group-collapse now session-persists across re-render (improvement vs Vue per-render reset; documented, NOT part of byte-identical claim).

DECISION 2026-07-02 (Scott): free-text YAML serializer — ADD `yaml` npm pkg as a TEST-ONLY devDependency (Obsidian's real stringifyYaml) so free-text byte-compat is faithful. Do it in the pre-Negotiation/Initiative kit-hardening pass (gating fix B).

################################################################
PLAN 05 — Kit hardening + Negotiation migration (5 tasks)
Plan: docs/superpowers/dse-overhaul/plans/2026-07-02-plan-05-kit-hardening-negotiation.md
Same branch dse-framework. Front-loading the hard path (Scott: maximize Fable before 2026-07-07).
Models: implementers=Fable, per-task reviewers=Fable, final whole-plan review=Opus.
Order: 1 kit-fix A (listener/modal accumulation) · 2 kit-fix B (yaml serializer + free-text golden) · 3 kit-fix C (.dse-error-card CSS) · 4 Negotiation model (byte-compat serialize) · 5 Negotiation element (view+register+retire processor).
*** CORRECTION (worktree survey §7): Negotiation has NO external refs — self-contained persisted tracker (tabs + menu/reset + free-text). The custom resolveRefs + field-merge on bare-path `statblock` is INITIATIVE-ONLY (Plan 06). README/handoff's "Negotiation/Initiative need custom resolveRefs" over-bundled. ***
BASE for Task 1: 186b6f4
################################################################

PLAN 05 Task 1 (kit listener/modal-close teardown): complete (commit 4586d0a, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). Child-Component per-cycle teardown in componentWrapper; renderContent(contentEl, contentOwner: Component); stamina-bar single activeModal closer registered in CONSTRUCTOR (correct vs onUpdate re-accumulation — reviewer verified view.update() re-runs onMount without unloading the view). 312/312 (+4 kit-lifecycle), tsc 0. Mock already had faithful child-Component teardown — no mock/collapsible changes. renderContent(contentEl, contentOwner) interface ready for Task 5.
  Minors (final-review triage): (1) stamina-bar/view.ts:151,160 activeModal not nulled on user-close — safe today (StaminaEditModal has no onClose override; detach of detached el is no-op) but latent if that modal ever gains a side-effecting onClose; null activeModal from the modal close path when next touched. (2) pre-existing test/dom/framework/kit/component-wrapper.test.ts still exercises the old 1-arg renderContent form (compiles/passes; modernize in a future kit pass).

PLAN 05 Task 2 (faithful yaml serializer + free-text golden): complete (commit 9034497, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). obsidian mock parseYaml/stringifyYaml now delegate to the `yaml` pkg at PURE DEFAULTS (matches Obsidian's stringify(value,null,{}); no options pinned — golden locks stability, faithfulness inherited from D1 T3 bundle inspection). New test/unit/model/yaml-roundtrip.test.ts (3 golden tests; non-tautological — RED discriminated js-yaml '>-' block-fold vs yaml plain multi-line). 315/315, tsc 0. Lockfile +2/-1 (root devDep `yaml`:^2.9.0 only, no re-bloat vs D1 T6 prune). Zero scalar-assertion drift; stamina-bar byte-compat guard green. Commit bodies verified trailer-free.
  Minors (final-review triage): (1) yaml-roundtrip.test.ts "faithfulness" rests on the bundle-finding, not a captured Obsidian save — add a one-line test-header note. (2) js-yaml + @types/js-yaml now DEAD/unused — prune (WITH the Task-1 component-wrapper.test.ts modernization) in the D-wave cleanup (F1 §6 step 10) or a numbered FOLLOWUPS.md entry.

PLAN 05 Task 3 (framework error-card CSS): complete (commit 6709709, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). styles-source.css only (+55). Every class/attr from pipeline.ts:103-125 styled; reviewer coverage-audited each. data-dse-error-stage is on the render ROOT (parent), NOT .dse-error-card — stage accents use `[data-dse-error-stage=…] > .dse-error-card` (child combinator; a compound selector would be dead — verified vs pipeline.ts). Obsidian vars only (theme-safe), aligned to legacy .error-message; semantic accents (parse/schema→orange, reference→blue, render→red). build-no-check emits rules; tsc 0. Commit trailer-free.
  Minors (final-review triage): (1) card bg uses --background-secondary vs legacy .error-message's --code-background — cosmetic, D3 token pass owns it. (2) grep -c on minified CSS counts lines not occurrences (verification-script footnote).

*** KIT HARDENING COMPLETE (Plan 05 T1-3, all reviews clean, 315 tests / tsc 0 @ 6709709). Negotiation (T4-5) + Initiative (Plan 06) migrations unblocked. ***

BYTE-COMPAT CONTRACT (verified for T4/T5): legacy negotiation writeback = CodeBlocks.updateNegotiationTracker → updateCodeBlock → stringifyYaml(data).trim() on the WHOLE NegotiationData instance (CodeBlocks.ts:102; canvas path :79). So serialize(model)=stringifyYaml(model).trim(), model via verbatim `new NegotiationData(data)`/parseNegotiationData. NOTE (T5): persistence is SCATTERED — sub-views (ArgumentView, MotivationsPitfallsView, PatienceInterestView) call CodeBlocks.updateNegotiationTracker(app,data,ctx) DIRECTLY (8+ sites); migration must re-wire them onto the view's persist() callback.

PLAN 05 Task 4 (Negotiation model — parse + byte-compat serialize): complete (commit 4e5e742, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). src/elements/negotiation/model.ts: parse = `new NegotiationData(data)` VERBATIM (no DTO); serialize = `stringifyYaml(model).trim()` (matches CodeBlocks.ts:102 whole-instance write; reviewer confirmed vs CodeBlocks:90-112). New test/unit/model/negotiation-serialize.test.ts (7 tests): legacy-oracle equality + 14-key top-level order pin + defaults pins (patience=5/interest=0, i5..i0 placeholders) + free-text folding (170-char reason forces indented fold, no '>-'; i0 |- literal w/ blank line; losslessness vs hardcoded JS constants) + round-trip idempotence (2 passes). 322/322, tsc 0. Commit trailer-free.
  Minors (final-review triage): legacy-oracle toBe assertions are code-path tautological BY DESIGN (brief: reusing class = the guarantee; discrimination lives in the hardcoded shape/order/folding pins). Optional future armor = a hand-authored golden snippet of frodo output. Non-blocking.
  T4-report Task-5 notes: (a) legacy "Invalid YAML format:" wrapper never fires — pipeline catches parse-stage first; error-card copy differs (acceptable). (b) empty-source new NegotiationData(null) TypeError preserved (legacy behavior).

T5 PREP (verified by reading NegotiationTrackerProcessor.ts + PatienceInterestView.ts): sub-views UPDATE THEIR OWN DOM in place on mutation (setPatience/setInterest toggle classes via querySelector) and call CodeBlocks.updateNegotiationTracker ONLY for persistence. So migration = reuse sub-views, swap the CodeBlocks call for an injected `persist:()=>void` (→ view.persist()); NO complex onUpdate rebuild needed except after Reset. Layout: name-line + reset menu (resetData→persist); tracker = PatienceInterestView + 2-tab actions (ArgumentView "Make an Argument" active-by-default / LearnMoreView "Learn Motivation/Pitfall"); details = MotivationsPitfallsView. Tabs = ephemeral DOM .active toggle today (reset on re-render; SessionStore-backing optional improvement). Sub-view ctors take (app,data,ctx); ArgumentView.build(container, root) [2 args]. DROP the manual mousedown/pointerdown click-shield (proc:33-34) + try/catch — framework provides both (default click-shield + renderErrorCard).

PLAN 05 Task 5 (Negotiation element — view+register+retire processor): complete (commits 8cd8481 + docs b811aa5, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). Negotiation on Framework v2; NegotiationTrackerProcessor.ts DELETED. definition (persisted, NO schema, autoResolveRefs:false, 3 aliases); view.ts (direct DOM — NOT collapsible; per-mount cycleOwner child Component for listener discipline; reset→framework update() rebuild→persist); sub-views re-wired to `(data, persist:()=>void)` — CodeBlocks calls swapped (ArgumentView×6, MotivationsPitfalls×1, PatienceInterest×2), CodeBlocks imports removed, dead app/ctx/root/fields dropped; framework click-shield armed by default (manual mousedown/pointerdown shield died w/ processor); .ds-nt-* CSS re-scoped via ONE nested `[data-dse-element="negotiation"] .ds-nt-container` selector (verified: 0 rules at col 0 — none leaked). Registered-exactly-once proven via real onload() spy; plugin-wiring.test.ts STRENGTHENED (adds 'negotiation'). Byte-compat proven at real replaceSource payload + FakeVault note-content level (4 mutation kinds + reset), oracle = legacy CodeBlocks.ts:102 literal. New test/dom/elements/negotiation.test.ts (21 tests, real ElementPipeline+ReadingModeBlockHost+FakeVault, fake-timer exactly-one-write incl. debounce coalescing). 343/343, tsc 0, build clean. Commits trailer-free.
  *** DELIBERATE IMPROVEMENT (test-pinned): legacy PERSISTED ON EVERY RENDER (sub-views called CodeBlocks during build → rewrote block + canonicalized fence alias on every open — a write-on-open bug). Migration: rendering performs ZERO writes; only user actions persist. First user action still writes the identical materialized-defaults shape (oracle asserts byte-equal). ***
  Minors (final-review triage): (1) canPersist=false is write-proof (test-pinned) but sub-view bubbles/checkboxes still toggle VISUALLY + mutate in-memory model (discarded on re-render) — LEGACY PARITY (cosmetic, not data-integrity); true read-only needs sub-view read-only mode → FOLLOWUPS + bounded future fix (stamina-bar disabled-input precedent). (2) Canvas persistence: framework quarantines sourcePath='' → canPersist=false (spec §9), so canvas negotiation is now read-only (legacy had a text-match canvas write fallback) — plan-mandated, ride the same FOLLOWUPS item, human decides. (3) view.ts reset chain `void this.update(model).then(()=>void this.persist())` has NO .catch — unhandled rejection + persist skipped if update() rejects, model already reset → in-memory diverges from note; add .catch/try (1-line). (4) main.ts frameworkV2 class-field comment still says "Horizontal Rule, Skills, Stamina Bar so far" (stale; add negotiation). (5) definition.ts:5 NegotiationData used only as type → `import type`.

################################################################
=== PLAN 05 (Kit hardening + Negotiation) — TASKS 1-5 COMPLETE, all per-task reviews clean (implementers+reviewers=Fable). ===
Branch dse-framework, increment 186b6f4..b811aa5 (6 commits: 4586d0a T1, 9034497 T2, 6709709 T3, 4e5e742 T4, 8cd8481+b811aa5 T5). 343 tests / tsc 0 / build clean. All commit bodies trailer-free.
Kit hardened (T1-3); Negotiation fully migrated onto Framework v2 (T4-5); NegotiationTrackerProcessor retired. 7 legacy elements remain: initiative (Plan 06 — the hard one, bare-path statblock resolveRefs) + the easy 6 (feature, featureblock, statblock, counter, characteristics, values-row — Plan 07).
ACCUMULATED PLAN 05 MINORS (for Opus final-increment review triage): T1(2), T2(2), T3(2), T4(1), T5(5) — see per-task entries above. Notable actionable: T5#3 (reset unhandled-rejection, 1-line), T5#1/#2 (read-only/canvas → FOLLOWUPS.md entry), T2#2 (js-yaml prune), T1#1 (activeModal null-on-close).
NEXT: Opus whole-increment review (186b6f4..b811aa5), then FOLLOWUPS.md entries, then Plan 06 (Initiative).
################################################################

=== PLAN 05 FINAL whole-increment review (opus): READY TO MERGE = YES ===
No Critical, no Important. All accumulated Minors safe to defer (none block landing or building on this increment). Opus verified: byte-compat airtight BY CONSTRUCTION (same NegotiationData instance + same writer expression, proven at real ReadingModeBlockHost+FakeVault vault-payload level, stable across all mutation kinds); kit per-cycle teardown correct for BOTH callers + self-limiting (ownedChildren spliced before re-add, no growth); render-never-writes = genuine footgun removal (legacy rewrote block + canonicalized fence on every open) that PRESERVES byte-compat on first user write; exactly-once registration + legacy coexistence intact; serializer swap blast radius bounded to free-text (scalar equivalence guarded by stamina-bar suite; no silently-shifted assertion found).
=== SOUND TO BUILD PLAN 06 (Initiative) ON THIS INCREMENT = YES. ===
Opus priority note: T5(c) reset-chain no-.catch is the highest-priority deferred item — fix before Plan 06 (Initiative leans on update()-rebuild harder).

PLAN 05 CLEANUP (dispatched, Fable, BASE b811aa5): T5(c) negotiation resetNegotiation → async try/catch (rebuild+persist; no unhandled rejection / silent persist-skip) + T5(d) main.ts frameworkV2 stale field comment + T5(e) definition.ts `import type NegotiationData` + T1 stamina-bar activeModal null-on-close.
DEFERRED to D-wave cleanup (F1 §6 step 10) / FOLLOWUPS.md at landing: js-yaml+@types/js-yaml prune (dead); negotiation sub-view read-only mode for canPersist=false (+ canvas-now-read-only release note); error-card --background-secondary vs --code-background token reconcile (D3 owns); component-wrapper.test.ts 1-arg→2-arg modernization; yaml golden-basis test-header note.

PLAN 05 CLEANUP: complete (commit cd73e70, spot-checked by controller — 33+/17-, 4 files, trailer-free, 343/343, tsc 0; Opus-baseline approved + these are Opus's own recommended fixes so no separate reviewer). (1) resetNegotiation async try/catch; (2) main.ts frameworkV2 JSDoc → points to registerFrameworkElementDefinitions; (3) definition.ts import type; (4) stamina-bar activeModal null-on-close via identity-guarded onClose wrap (single-closer invariant held). Report note: if StaminaEditModal ever assigns an instance-level onClose post-construction it'd clobber the wrapper (move hook into modal then).

################################################################
=== PLAN 05 FULLY LANDED & CLEAN @ cd73e70. Increment 186b6f4..cd73e70 (7 commits). 343 tests / tsc 0 / build clean. Opus: ready-to-merge=YES, sound to build Plan 06 on. NOT yet wt-finished (Scott's call, deploy separate). ===
################################################################

################################################################
PLAN 06 — Initiative Tracker migration (5 tasks, lean plan)
Plan: docs/superpowers/dse-overhaul/plans/2026-07-02-plan-06-initiative-migration.md
Same branch dse-framework. Models: implementers=Fable, reviewers=Fable, final=Opus.
Order (deps): 1 model (sync parse split from parseEncounterData + serialize) → 2 resolveRefs (additive ReferenceService.resolveBarePath + def.resolveRefs field-merge for bare statblock) → 3 MinionStaminaPoolModal decouple + FIX CB-1/CB-2 (flip 3 test.failing nets) → 4 view (big, port ~533-line processor, wire 5 modals, register, retire processor; MAY SPLIT 4a/4b) → 5 CSS re-scope (~960 scattered lines; modal CSS stays body-reachable).
Initiative facts (survey 2026-07-02): only bare-path statblock resolver; ~1560 lines; 13 CodeBlocks sites in processor + 1 in modal; async model (statblock resolution); EncounterData = {heroes, enemy_groups, malice}; parse must be SYNC so ref-resolution splits into resolveRefs; modals 4/5 already callback-based (reuse verbatim), only MinionStaminaPoolModal coupled.
🔵 OPEN DECISIONS — SCOTT (in plan §"Open Decisions"): OD-1 canvas persistence regression (USER-VISIBLE: initiative-on-canvas becomes read-only — rec accept + release note; HOLD before Task 4); OD-2 resolveRefs approach (rec additive resolveBarePath); OD-3 findFile step-5 sourcePath (rec "" legacy-exact); OD-4 fix CB-1/CB-2 here (rec yes). OD-2/3 bite in Task 2; OD-1/4 in Tasks 4/3. Proceeding on rec defaults; OD-1 (user-visible) explicitly held for Scott before Task 4.

PLAN 06 Task 1 (Initiative model — sync parse split + byte-compat serialize): ATTEMPT 1 hit the account usage limit (~5:4Xpm ET, reset 5:50pm) during its reading phase — clean tree, NO commit, nothing salvaged. Re-dispatched ~6:19pm ET (after reset) → complete (commit b8e6014, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). src/elements/initiative/model.ts (258) + test/unit/model/initiative-serialize.test.ts (348; 26 tests; legacy-oracle byte-compat [calls the UNCHANGED async parseEncounterData on ref-free fixtures] + two-sided split-boundary). parse = legacy parseEncounterData MINUS (parseYaml wrapper, ReferenceResolver ctor :92, 2 statblock merges :110-128/:211-228, 4 post-merge name/max_stamina validations :130/:133/:230/:240); KEEPS all merge-independent (shape, conditions×3, hero defaults :156-159, group checks, squad-role :174-208, amount, instance materialization :249-343, malice). serialize = stringifyYaml(model).trim(). EncounterData.ts UNTOUCHED. 369/369, tsc 0. Reviewer verified the split line-by-line vs :82-353.
  *** LOAD-BEARING for Task 2 (T1 report + review): resolveRefs MUST, AFTER the statblock merge, in this order: (1) run the 4 deferred name/max_stamina validations for ALL heroes/creatures (not just ref-bearing) with EXACT legacy messages (:130/:133/:230/:240); (2) IDEMPOTENTLY re-apply the max_stamina-dependent fills parse could not complete when max came from a statblock — hero current_stamina ?? max_stamina + temp ?? 0; per-instance current_stamina ?? max; minion_stamina_pool == null → max×amount (parse's NaN-guard SKIPS pool init when max non-numeric, so this post-merge re-init is REQUIRED). All re-applications must be no-ops for the common (explicit-max) case. Do NOT strip statblock strings. ***
  Minors (final-review triage): (1) model.ts NaN-guard comment "unreachable for ref-free" imprecise (reachable for ref-free minion missing max; neutral only because T2 validates-all first) — add a sentence noting the T2 coupling; (2) test lacks a pin for the instance-branch condition-format message (hero pinned); (3) error-precedence on multiply-broken input differs cosmetically (amount-before-name; undefined interp) — forced by the deferral, all end in error card, disclosed.
  *** TASK-BOUNDARY REFINEMENT: definition.ts needs createView→InitiativeView (Task 4), so it can't compile in Task 2. Task 2 delivers resolveRefs as a STANDALONE tested function `resolveInitiativeRefs(model, refs)` (src/elements/initiative/resolveRefs.ts) + the additive framework `ReferenceService.resolveBarePath`. Task 4 assembles definition.ts (parse+serialize+resolveInitiativeRefs+InitiativeView) + registration. ***

PLAN 06 Task 2 (bare-path statblock resolveRefs — resolveBarePath + resolveInitiativeRefs): implemented (commit 2d1f960, review: Spec ✅ / quality Approved, 1 IMPORTANT + 3 Minor; implementer+reviewer=Fable). resolveBarePath (additive framework method; sourcePath="" spy-pinned = legacy-exact; NO provider-chain touch [resolveDeep untouched]; @/[[/bare dispatch parity with legacy resolveReferences) + resolveInitiativeRefs (merge only-if-unset + `+stamina` coerce; statblock string preserved; 4 name/max_stamina validations byte-exact via legacy-parseEncounterData ORACLE; idempotent post-merge fills [pool ==null→max×amount, ?? max] proven ref-free BYTE-IDENTICAL across quick-start/squad/mid-encounter). New test/fixtures/initiative/statblock-refs.yaml; 28 new tests. 397/397, tsc 0. Reviewer notes: legacy bugs preserved (ref overwrites explicit 0; NaN from +"abc" passes typeof); resolved?.data guard correct; @/[[ stripping is REQUIRED fidelity (not a divergence); ref-bearing first-write key-order diff = value-neutral, converges byte-stable. Trailer-free.
  IMPORTANT → FIXING (implementer+reviewer both flagged): resolveBarePath returns null on not-found/no-block → merge silently skipped → user sees 'Hero at index 0 missing name' instead of the legacy not-found hint (misleading for the common typo/rename case). FIX: resolveBarePath THROWS on not-found + no-block (resolveByPath already produces those messages — stop catching them into null), returns null ONLY for parses-to-null; the merge's existing catch (resolveRefs.ts:60-70/85-93) wraps into the byte-exact 'multiple instances…/full path' hint. Legacy distinguishes: not-found→throw→hint; parses-to-null→skip→no error. Update the dangling-ref test (initiative-resolve-refs.test.ts:314-327) to expect the legacy hint.
  Minors (final-review triage): (2) resolveBarePath name undersells the @/[[ dispatch — Task 4 must NOT add a 2nd dispatch layer over statblock strings; (3) hero/creature merge bodies near-dup — post-compat-lock shared helper; (4) F1 §3.7 spec-DOC amendment owed (in-code interface doc done; orchestrator to sync the F1 doc for resolveBarePath).

PLAN 06 Task 2 FIX (resolveBarePath throw-on-miss): complete (commit af770b3, spot-checked by controller — reviewer-prescribed + oracle-verified + test-covered, so no separate re-review subagent). resolveBarePath delegates to resolveByPath → throws byte-exact legacy not-found/no-block/malformed-YAML messages (merge try/catch wraps into "multiple instances/full path" hint, oracle-verified); returns null ONLY for parses-to-null (EncounterData.ts:114 silent-skip). Dangling-ref test → 3 legacy-byte-equal pins; refs.test null-on-miss → throw assertions + parses-to-null pin. 400/400, tsc 0, trailer-free.
=== PLAN 06 Task 2 COMPLETE (impl 2d1f960 + fix af770b3, review clean w/ Important resolved). The bare-path statblock resolveRefs crux is done. ===

PLAN 06 Task 3 (decouple MinionStaminaPoolModal + fix CB-1/CB-2 → flip test.failing nets): complete (commit 61644ff, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). CB-1 fixed (:226 → `(len ?? 0) * max`; net asserts pool=17, was 5 under the bug). CB-2 fixed (empty-language CodeBlocks call GONE; condition-removal → persist callback; net proves fence `ds-initiative` survives). Modal decoupled: no CodeBlocks import; signature `(app, group, creature, persist:()=>void)` — data+ctx were persistence-plumbing-only (fed buildConditionIcons passthrough + the CodeBlocks call), dropped correctly. Legacy initiativeProcessor.ts callers (:323/:374) drop the 2 args, callback bodies byte-identical → behavior preserved (condition-removal now writes proper fence language). CB-1/CB-2/DC-6 nets flipped test.failing→test, PASSING against a real persist-to-fake-vault path (end-to-end fence+data survival, not spy-only). grabbed-scoping = legitimate fix of an unsatisfiable assertion. 400/400, tsc 0, trailer-free.
  *** Task-4 carry-forward: the modal's `persist` callback must BOTH write AND refresh owner UI — Task 4's view callback must do both. ***
  Minors (final-review triage): (1) test enemy_groups: index guard (indexOf -1 → slice(-1) silent vacuous pass; add `expect(updated).toContain('enemy_groups:')` before slice); (2) CB-2 `not.toContain('```\n```')` decorative (the `toContain('```ds-initiative')` assertion is load-bearing).

################################################################
=== PLAN 06 Tasks 1-3 COMPLETE (model + statblock resolveRefs + modal-decouple/CB-fixes). Branch dse-framework, af770b3→61644ff via 2d1f960/af770b3/61644ff. 400 tests / tsc 0. 3 data-corruption nets (CB-1/CB-2/DC-6) now GREEN. ===
=== OD-1 RESOLVED 2026-07-02 (Scott): ACCEPT read-only on canvas (defer true canvas persistence — blocked on an Obsidian API gap: no stable canvas-node handle from the render ctx, only the fragile text-match legacy used). REQUIREMENT ADDED: the UI must CLEARLY INDICATE read-only (Scott — no silent no-save). See memory [[explicit-readonly-indication]]. ===
=== REVISED TASK ORDER: Task 3.5 read-only affordance (framework, reusable) → Task 4 Initiative view → Task 5 CSS re-scope. ===

PLAN 06 Task 3.5 (read-only affordance): complete (commit c94f236, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). pipeline.ts:170 one-line stamp `if(!host.canPersist) root.setAttribute('data-dse-readonly','true')` (between data-dse-element stamp + click-shield; additive; pinned absent when canPersist=true + an additivity guard re-asserting element/theme attrs). styles-source.css:478-499 CSS-only ::after "Read-only" pill (Obsidian vars only, scoped [data-dse-readonly], pointer-events:none, z-index:1; root position:relative). Reviewer independently confirmed position:relative safe for ALL migrated elements' absolute descendants. 402/402, tsc 0, trailer-free.
  Minors (final-review triage / D3 polish): (1) VISUAL-QA (not a code change): kit LP rule styles-source.css:2064 (.ds-kit-collapsible-heading > .heading-collapse-indicator {position:absolute}) has no positioned ancestor in the DSE subtree → the new root position:relative re-anchors it, but ONLY on read-only+Live-Preview skills-collapse renders (few-px shift likely). Spot-check a skills block w/ collapsible group headings on a read-only render when Initiative lands; if it drifts, fix = `.ds-kit-collapsible-heading { position:relative }`. (2) badge shows on DISPLAY-ONLY elements too (HR/skills on canvas get a misleading "Read-only" pill — they never persist); more-correct = gate the stamp on `def.shape === "persisted" && !canPersist` (1-line; def is in scope at pipeline.ts:170) — deferred to D3/polish. (3) content:'Read-only' hard-codes English (no i18n today).

=== TASK RENUMBER (view too big for one task): Task 4 = build full InitiativeView + definition UNREGISTERED (legacy processor keeps serving users; tested via pipeline.run directly) → Task 5 = register + retire processor (small flip) → Task 6 = CSS re-scope (was plan Task 5). ===

PLAN 06 Task 4 (Initiative view + definition, UNREGISTERED): complete (commits d02f50f view+def + add1cda test, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). InitiativeView (604 lines) — all 9 legacy build/update methods ported; ALL 13 CodeBlocks→persist swaps verified line-by-line (reviewer mapped each to its legacy line); in-place DOM updates preserved; 5 modals wired (4 verbatim, MinionStaminaPoolModal via (app,group,creature,persist) with refresh+save callbacks; single view-lifetime activeModal closer, identity-guarded onClose, no accumulation); reset via resetEncounter→update() rebuild→persist w/ async try-catch; click-shield+try/catch dropped (framework provides); read-only STRONGER than inert (write affordances not built at all when !canPersist; test drives clicks → ZERO writes + data-dse-readonly present). definition.ts (persisted, no schema, autoResolveRefs:false, resolveRefs=resolveInitiativeRefs, UNREGISTERED — no main.ts/RegisterElements change, legacy processor intact). Test (719 lines, real ElementPipeline+ReadingModeBlockHost+FakeVault): render both fixtures, exactly-one byte-compat write per 8 interactions, both minion paths, statblock-refs end-to-end incl. dangling-ref hint, read-only, alias-not-rewritten + surrounding-note-intact. 426/426, tsc 0, trailer-free.
  Two DIVERGENCES verified SOUND: (a) malice text → `.malice-text` not container (legacy setText wiped chevrons; masked by legacy's write-re-render; REQUIRED under debounced persistence; malice.value byte-untouched). (b) Reset shows reset-shape (0/max) until re-render (legacy wrote reset bytes with NO rebuild → persisting serialize(reset model) IS legacy bytes; display-only transient).
  *** Task-5 carry-forwards: (1) remove RegisterElements.ts:29-33 (+ InitiativeProcessor import :1) BEFORE/when registering (else double-registration); (2) flip the "not registered" test pin (initiative.test.ts:147-155) to registered-exactly-once; (3) deleting initiativeProcessor.ts ORPHANS test/dom/elements/initiative-render.test.ts (legacy) → delete it (initiative.test.ts replaces it); (4) plugin-wiring.test.ts add 'initiative' to expected registry; (5) optional: relax RegisterElements `plugin` param to `Plugin` (concrete-type rationale :8-14 was InitiativeProcessor-only). ***
  Minors (final-review triage / FOLLOWUPS): (1) buildUI reset-round uses this.model vs the `data` param elsewhere (cosmetic); (2) Reset transient 0/max flash → FOLLOWUPS; (3) nested CustomizeConditionModal not closed on view unload (needs ConditionSelectModal touch, out-of-scope; legacy had none) → Task-5+/FOLLOWUPS; (4) read-only loses instance-browsing (cell click gated; in-memory select would keep browsing) → polish; (5) test could add a post-coarse-rebuild write-once pin (listener re-accumulation guard); (6) floating Images.resolve().then() no .catch = verbatim legacy CB-14 parity (keep on CB-14 ledger).

PLAN 06 Task 5 (register initiativeElement + retire legacy processor): complete (commit 53a4006, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). main.ts:33/92 import+register (5th def); RegisterElements.ts ds-it* 4 lines + InitiativeProcessor import removed ATOMICALLY (house-style migrated comment) — no double-reg window. Deleted initiativeProcessor.ts (533) + orphaned legacy initiative-render.test.ts (70, 4 tests). Registration pin flipped → T-5 registered-EXACTLY-ONCE (real onload() spy w/ BOTH paths live: leftover legacy would make per-alias count 2; registry.get('ds-it').id==='initiative'). plugin-wiring.test.ts: 'initiative' added to both expected-id arrays; LEGACY_ALIASES ds-initiative-tracker→ds-statblock (else vacuous coexistence — reviewer verified ds-statblock still legacy). Param relax done (RegisterElements plugin: DrawSteelAdmonitionPlugin→Plugin — was InitiativeProcessor-only; removes main↔RegisterElements import cycle). counter-view.test.ts (comment-only): re-pointed a dangling harness-doc ref off the deleted initiative-render.test.ts. definition.ts: stale 'NOT REGISTERED' header updated (comment-only). 423/0, tsc 0, build OK, trailer-free. No dangling non-comment refs.
  Minors: (1) view.ts:2 still cites initiativeProcessor.ts:58-532 (deleted file) — reword in final cleanup; (2) report typo 585 vs 533 (cosmetic).

################################################################
=== PLAN 06 Tasks 1-5 COMPLETE. Initiative FULLY MIGRATED onto Framework v2 (model + statblock resolveRefs + view + registration); legacy 533-line processor + CodeBlocks-for-initiative retired; CB-1/CB-2 data-corruption bugs FIXED (nets green). Only Task 6 (CSS re-scope) + Opus final whole-increment review remain. ===
################################################################

PLAN 06 Task 6 (initiative CSS re-scope): complete (commit e5680ae, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). styles-source.css only (38+/28−): 28 in-tracker selectors prefixed `[data-dse-element="initiative"]` (prefix-in-place; source-order/cascade/declarations provably unchanged; group selectors both-members prefixed). Modal blocks (.minion-stamina-modal + internals, .stamina-header, .stamina-bar-*, .apply-*, .action-button-*, .add-condition-modal, .customize-condition-*, condition-list/item) + shared (.condition-icon, .add-condition-icon, .condition-effect-* + keyframes) + .ds-container left GLOBAL. Reviewer independently grepped all 27 scoped classes → each resolves ONLY to view.ts (none in *Modal.ts/SettingsTab); scope attr live (pipeline.ts:169 stamps data-dse-element=def.id; id='initiative'). build OK, 423/423, tsc 0, trailer-free. NO automated CSS test → manual Obsidian visual QA deferred (checklist in report).
  Minors: (1) .creature-group scoped-though-DEAD (no construction site) — FOLLOWUPS candidate (delete the dead rule); (2) specificity +1 uniform, no override flips (no action).

################################################################
=== PLAN 06 Tasks 1-6 COMPLETE. Initiative FULLY MIGRATED onto Framework v2 (model + statblock resolveRefs + view + registration + CSS re-scope). Legacy 533-line processor + CodeBlocks-for-initiative retired; CB-1/CB-2 data-corruption bugs FIXED. Branch dse-framework, increment cd73e70..e5680ae (Task commits: b8e6014 T1, 2d1f960+af770b3 T2, 61644ff T3, c94f236 T3.5, d02f50f+add1cda T4, 53a4006 T5, e5680ae T6). 423 tests / tsc 0 / build OK. All commits trailer-free. ===
=== NEXT: Opus final whole-increment review (cd73e70..e5680ae), then land decision + Plan 07 (easy 6). ===
################################################################

=== PLAN 06 FINAL whole-increment review (opus): READY TO LAND = WITH FIXES (one docs fix, now DONE) ===
No Critical, no Important CODE issues. Opus verified: parse/resolveRefs split faithful to the legacy oracle end-to-end (proven, not asserted, in initiative-serialize + initiative-resolve-refs tests); byte-compat proven through the REAL ReadingModeBlockHost+FakeVault at all 13 persist sites + resets; NaN-guard T1→T2 handoff coherent (pool undefined→post-merge init, idempotent re-fills no-op on explicit-max); framework touches provably ADDITIVE (resolveBarePath bypasses provider chain — resolveDeep untouched; read-only = pure attr stamp + CSS, per-element write-gating unchanged); registration exactly-once + coexistence intact (registry [hr,skills,stamina-bar,negotiation,initiative]); CB-1/CB-2 genuine fixes; both accepted divergences (malice-text, reset transient) byte-compat-preserving; tsc 0 across integrated tree.
=== SOUND TO CONTINUE TO PLAN 07 (remaining 6 legacy elements) = YES. === (Opus: this exercised the hardest surface — async-ref→sync-parse+resolveRefs, 13 persist sites, 5 modals, squad math, read-only — and the framework absorbed it additively; the per-mount owner / single modal closer / rebuildAndPersist / injected-persist decouple / legacy-oracle test methodology are the reusable patterns Plan 07 needs.)
FIX-BEFORE-LAND (the only one, Important, DOCS): F1 §3.7 spec amendment for resolveBarePath → **DONE** by orchestrator (F1-element-framework-v2-spec.md §3.7: added the method to the ReferenceService interface + an amendment paragraph — direct method not a provider, sourcePath="", initiative-only field-scoped).

DEFERRED → FOLLOWUPS.md (owed at landing/cleanup; Opus triaged ALL as safe-to-defer, none block Plan 07): 
  [P06] (a) read-only badge+position:relative reach NON-persisted migrated elements (HR/skills get a badge on canvas; position:relative is the likely cause of the T3.5 skills-collapse-indicator LP-anchor shift) → gate the badge/position on shape==="persisted" + DO A CANVAS VISUAL-QA on skills (the one change touching already-migrated elements); (b) resolveRefs hero/creature merge near-dup → mergeStatblock helper (fold into Plan 07 scaffolding); (c) view.ts buildUI this.model vs data param (cosmetic); (d) reset transient 0/max flash; (e) nested CustomizeConditionModal not closed on view unload; (f) view.ts:2 cites deleted initiativeProcessor line numbers; (g) .creature-group dead CSS = clean deletion candidate; (h) T1 NaN-guard comment imprecision + missing instance-branch condition-format test pin; (i) floating Images.resolve().then() no .catch = CB-14 (verbatim legacy parity).
  [P05, still owed] js-yaml+@types/js-yaml prune; error-card --background-secondary vs --code-background (D3); component-wrapper.test.ts 1-arg modernization; negotiation canvas-read-only release note; yaml golden-basis test-header note; negotiation sub-view read-only mode (now largely covered by the T3.5 affordance).

################################################################
=== PLAN 06 COMPLETE & CLEAN @ e5680ae (increment cd73e70..e5680ae, 9 commits, 423 tests / tsc 0 / build OK, all trailer-free). Initiative fully migrated. Opus: ready-to-land with the one docs fix (DONE). NOT wt-finished (Scott's call). ===
=== D-WAVE STATUS: 5 of 11 elements on Framework v2 (HR, Skills, StaminaBar [D1] + Negotiation [P05] + Initiative [P06]). Remaining 6 = Plan 07: Feature→Featureblock→Statblock (SDK static), Counter (persisted), Values Row + Characteristics (trivial static). ===
=== ⚠️ WORKSPACE-DOC HYGIENE (for landing): docs/superpowers/dse-overhaul/ is UNTRACKED in the main checkout (was `??` at session start) — plan-05, plan-06, plan-07, F1 §3.7 amendment, README build-progress all live there uncommitted. At landing: `git add docs/superpowers/dse-overhaul/` + commit to the workspace superproject, AND file the FOLLOWUPS.md bundle above. Plan docs are on the main checkout, NOT the worktree branch. ===
################################################################

################################################################
PLAN 07 — the remaining 6 elements (5 tasks).
Plan: docs/superpowers/dse-overhaul/plans/2026-07-03-plan-07-easy-elements.md
Same branch dse-framework. Models: implementers+reviewers=Fable (Scott's standing instruction; the easy 6 are MECHANICAL so I offered Sonnet-to-conserve-Fable — awaiting Scott's steer, defaulting Fable per my stated commitment), final review=Opus.
Order: 1 Feature (FIRST — FB+Statblock reuse its FeatureView/EffectView/FeaturesView) → 2 Featureblock → 3 Statblock → 4 Counter (persisted, stamina-bar shape) → 5 ValuesRow+Characteristics (trivial static, paired, shared CSS).
Survey (2026-07-03): all static except Counter (persisted); ALL schema-less + NO refs; SDK-backed 3 parse from RAW (`<Config>.readYaml(raw)`, NOT pre-parsed data; FeatureConfig double-parses for .indent — reuse verbatim); plain 3 use `.parse(data)`. *** CSS: shared sub-view classes (.ds-feature-container/.ds-features/.ds-header-container/.ds-bkv-container/.ds-hr-container/.ds-pr-*/.ds-multiline/.ds-container) stay GLOBAL — used by negotiation:1700-1707/statblock:1838/featureblock:1905 + multiple ids; do NOT naive-rescope (the HR .ds-hr-container hazard). Only Counter (:1946-2003) re-scopes cleanly; ValuesRow+Characteristics share :2004-2050. *** Keep shared builders (Features/*, Common/HeaderView/BoldKeyWithValueView/horizontalRuleProcessor) in place + import. ValuesRow noClickShield:true (legacy doesn't shield). End state: RegisterElements.ts EMPTY, all 11 on framework.
BASE for Task 1: e5680ae.

PLAN 07 Task 1 (Feature migration): complete (commit 8eb6815, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). elements/feature/{definition,view}.ts; parse=(_data,raw)=>FeatureConfig.readYaml(raw) [SDK RAW; indent double-parse reused verbatim]; static, no schema/serialize/refs; view reuses FeatureView.build BYTE-IDENTICALLY (golden DOM-equality test). ctx SHIM `{sourcePath: host.sourcePath}` — Feature sub-view tree reads ONLY ctx.sourcePath (grep-verified). FeatureProcessor.ts deleted; shared Features/{FeatureView,EffectView,FeaturesView} KEPT (FB/Statblock reuse). NO CSS touched (shared .ds-feature-*/.ds-pr-*). Registered-once via real onload(); plugin-wiring updated (standard). 440/440, tsc 0, build OK, trailer-free.
  *** Carry-forward (FB/Statblock): reuse the {sourcePath} ctx shim; RE-AUDIT if their sub-views read getSectionInfo/ctx.el; reviewer suggests hoisting a shared narrow-typed shim helper (Pick<MPPCtx,'sourcePath'>) at the Statblock task. ***
  Minors: (1) feature.test.ts:308 decorative vault write (source arg renders; harmless); (2) view.ts:28 `as MarkdownPostProcessorContext` cast — narrow-type when hoisting the shim.
################################################################

PLAN 07 Task 2 (Featureblock migration): complete (commit e802652, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). elements/featureblock/{definition,view}.ts; parse=(_data,raw)=>FeatureblockConfig.readYaml(raw) [SDK RAW; no indent double-parse — FB has no indent key]; static, no schema/serialize/refs; view reuses FeatureblockView.build byte-identical (2 golden tests incl. a WITH_STATS stats-path); {sourcePath} shim (FB sub-views re-audited sourcePath-only). FeatureblockProcessor.ts deleted; sub-views + CSS untouched. Registered-once (real onload). 457/457, tsc 0, build OK, trailer-free.
  Carry-forward (Statblock/T3): fresh ctx audit (wider tree: StatsView/HeaderView/FeaturesView/horizontalRuleProcessor); FOLD StatblockProcessor.buildUI into the view (no legacy StatblockView class); SWAP the ds-statblock legacy-sample alias in plugin-wiring LEGACY_ALIASES/registry-absence asserts → a still-legacy alias (ds-ct/ds-char/ds-vr).
  Minor: shield test only mousedown not pointerdown (optional; shield is shared pipeline infra).

PLAN 07 Task 3 (Statblock migration): complete (commit 440e1f9, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). elements/statblock/{definition,view}.ts; parse=(_data,raw)=>StatblockConfig.readYaml(raw) [SDK RAW, no ref resolution]; static; view FOLDS StatblockProcessor.buildUI VERBATIM (HeaderView+StatsView+[HR+FeaturesView via features.map(new FeatureConfig)]; exact Level/Role/Ancestry/EV fallback strings + features?.length>0 guard); golden DOM-equality both feature-bearing + featureless; {sourcePath} shim. StatblockProcessor.ts deleted; StatsView + shared sub-views kept; no CSS. plugin-wiring legacy-sample ds-statblock→ds-vr + 'statblock' added; RegisterElements 17-line change fully accounted (no churn; refreshed stale Feature/FB comments). Registered-once. 475/475, tsc 0, build OK, trailer-free.
  Note: error-path DOM changed intentionally (framework renderErrorCard at root, NO .ds-sb-container wrapper — vs legacy wrapping first; test-pinned, not a regression).
  Carry-forward (Counter/T4): PERSISTED (shape:persisted + serialize=stringifyYaml(m).trim() + swap 3 updateCounter→persist + canPersist read-only + move raw addEventListener→registerDomEvent/contentOwner); re-scope the SELF-CONTAINED counter CSS (:1946-2003) under [data-dse-element="counter"] (safe, no sharing); add 'counter' to migrated lists (ds-vr stays the legacy sample).
=== SDK-backed three (Feature/Featureblock/Statblock) DONE. 3 of 6 Plan-07 elements migrated (Feature, Featureblock, Statblock). Remaining: Counter (T4, persisted) + ValuesRow/Characteristics (T5, paired). ===

PLAN 07 Task 4 (Counter migration — persisted): complete (commit afedb2a, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). elements/counter/{definition,model,view}.ts; parse=(data)=>Counter.parse(data) [plain]; serialize=stringifyYaml(m).trim() [byte-compat BY CONSTRUCTION — reuses Counter model, key order pinned]; persisted. View ports CounterView 1:1 (value/click-edit-input/± buttons); 3 updateCounter→persist (exactly-once debounced, coalescing pinned); raw addEventListener→registerDomEvent; canPersist=false→inert+disabled+zero-writes+data-dse-readonly; stable-two-node replaceWith swap; Enter-then-late-blur editing guard. CounterProcessor+CounterView+counter-view.test.ts DELETED. CSS re-scoped [data-dse-element="counter"] (self-contained, grep-verified). dom-setup.ts=comment-only (stale ref fix). plugin-wiring: 'counter' added, ds-counter removed from LEGACY_ALIASES (now ['ds-characteristics','ds-vr']), ds-vr untouched. 494/494 (−4 legacy +23), tsc 0, build OK, trailer-free.
  Minor: finishEditing persists even on no-op revert (legacy parity; harmless).
  *** Carry-forward (T5 = LAST; migrates BOTH ds-vr + ds-char → RegisterElements.ts EMPTY): plugin-wiring LEGACY_ALIASES becomes EMPTY → the `registry.get('ds-vr').toBeUndefined()` absence-assert + the coexistence-sample assert must be REMOVED/adjusted (no legacy left to coexist with — assert RegisterElements registers nothing instead); add 'values-row'+'characteristics' to migrated toEqual lists; ValuesRow `noClickShield:true` (legacy doesn't shield); ValuesRow+Characteristics SHARE the :2004-2050 CSS block (.ds-characteristics-container, .ds-values-row-container) → combined selector or leave global. ***
=== 4 of 6 Plan-07 elements done (Feature/Featureblock/Statblock/Counter). ===

PLAN 07 Task 5 (Values Row + Characteristics — LAST; empties RegisterElements): implementer COMMITTED (e40ad2c) then hit the account usage limit before reporting (reset 1:40pm ET). CONTROLLER-VERIFIED post-limit: 41 suites / 526 passed (was 494, +32), tsc 0, trailer clean, tree clean. (task-5-report.md holds STALE Plan-06 content — implementer didn't overwrite before the limit; rely on the commit body.) Both migrated: values-row (plain KeyValuePairs.parse; noClickShield:true; reuses ValuesRowView via {sourcePath} shim; ds-values-row-ele-container) + characteristics (plain Characteristics.parse; shield default; reuses CharacteristicsView; ds-characteristics-ele-container). Both processors DELETED; both Views kept. main.ts registers both → framework registry holds ALL 11. RegisterElements.ts registers NOTHING (empty documented stub). plugin-wiring REWORKED: retired LEGACY_ALIASES coexistence sampling → positive end-state asserts (registerElements registers 0 processors; every alias resolves in the framework registry; exact 11-element list). styles-source.css: shared Characteristics/ValuesRow block combined-scoped [data-dse-element="values-row"],[data-dse-element="characteristics"]. Review clean — Spec ✅ / quality Approved (reviewer=Fable, HIGH scrutiny on the plugin-wiring rework). Reviewer confirmed the rework is STRICTLY STRONGER (live jest.spyOn no-op assert on registerElements; exact 11-id toEqual in BOTH the registry test + the bad-schema resilience test; bidirectional alias↔registry set-equality + a registered.length>0 non-vacuity guard; ds-vr negative correctly inverted to positive) — NOT gutted. noClickShield asymmetry correct+pinned (values-row bubbles-to-document===1, characteristics===0); plain parse (garbage-raw pin); golden view reuse (root.innerHTML vs direct *View.build); CSS combined-scope w/ nested @media inheriting scope; RegisterElements empty documented stub (main.ts call site untouched). 526/526, tsc 0, build OK.
  Minors → FOLLOWUPS: (1) KeyValuePairs error message says "effects" not "values" (pre-existing model copy-paste quirk the test merely pins); (2) shield attach point on framework root vs legacy inner container (functionally equivalent — no action).

################################################################
=== PLAN 07 (6 remaining elements) — ALL 6 TASKS COMPLETE, all reviews clean (implementers+reviewers=Fable). Feature/Featureblock/Statblock/Counter/ValuesRow/Characteristics migrated. ===
=== *** ALL 11 ELEMENTS ON FRAMEWORK V2. RegisterElements.ts registers NOTHING (verified: 0 processors; main.ts registers 11). *** ===
Branch dse-framework, Plan-07 increment e5680ae..e40ad2c (5 commits: 8eb6815 Feature, e802652 Featureblock, 440e1f9 Statblock, afedb2a Counter, e40ad2c ValuesRow+Characteristics). 526 tests / tsc 0 / build OK, all trailer-free.
NEXT: Opus final whole-increment review (e5680ae..e40ad2c), then the D-WAVE ELEMENT MIGRATION IS COMPLETE (D1+P05+P06+P07 = all 11 elements; Vue gone; legacy processors + RegisterElements retired).
################################################################

=== PLAN 07 FINAL whole-increment review (opus): READY TO LAND = YES ===
No Critical, no Important. ALL Minors defer (none block landing). Opus independently verified: the ONLY live registerMarkdownCodeBlockProcessor call in src/ is the framework wiring point (registerFrameworkElements.ts:46) — everything else is a comment; RegisterElements.ts = 0 live calls (documented empty stub, main.ts call retained for step-10); all 11 defs carry UNIQUE non-colliding aliases (27 total), the 6 new match the deleted processors byte-for-byte; registry throws on dup id/alias/persisted-without-serialize (exactly-once enforced at the type layer); ctx-shim provably safe (directory-wide sweep: ctx read in exactly 4 places, all ctx.sourcePath); CSS discipline correct (shared classes stay global — verified cross-element consumption; re-scoped blocks verified self-contained); Counter byte-compat by construction + real host+vault pinned. No regression to the 5 prior elements.
=== *** D-WAVE ELEMENT MIGRATION COMPLETE (Opus-confirmed): all 11 elements on Framework v2; all 6 Plan-07 legacy processors + legacy CounterView deleted; RegisterElements empty. *** ===
Opus deferred-Minors (→ FOLLOWUPS / step-10 cleanup): CodeBlocks.updateCounter now DEAD (sweep at step-10); `as MarkdownPostProcessorContext` shim cast duplicated in 5 views → hoist a sourcePathCtx(host) helper; reused sub-views bind markdown children to plugin-as-Component not the view (PRE-EXISTING lifecycle quirk, byte-faithful — future D-cleanup); featureblock shield test mousedown-only; KeyValuePairs "effects"-not-"values" message.

################################################################
=== PLAN 07 COMPLETE & CLEAN @ e40ad2c (increment e5680ae..e40ad2c, 5 commits, 526 tests / tsc 0 / build OK, trailer-free). Opus: ready-to-land=YES. ===
=== 🏁 D-WAVE ELEMENT MIGRATION COMPLETE: ALL 11 ELEMENTS ON FRAMEWORK V2. Vue gone; every legacy processor retired; RegisterElements.ts empty. Built entirely on Fable (impl+review) + Opus (per-plan final review). NOT wt-finished (Scott's call). ===
=== FULL dse-framework BRANCH: b80a8a9 (pre-work) → e40ad2c. Plans: 01 harness, 02 framework-core, 03/D1 (Vue removal + first 3), 05 (kit+Negotiation), 06 (Initiative), 07 (easy 6). ===
################################################################

################################################################
D2 — UI/UX OVERHAUL (High-Fantasy Steel). Spec: docs/superpowers/dse-overhaul/D2-ui-ux-overhaul-spec.md.
Scott (2026-07-05): "we have time, just work through it" → proceed on ALL 8 OD recommendations: OD-1 fresh `dse-*` BEM; OD-2 role/action accents STEEL-ONLY (Legacy monochrome); OD-3 no hover-lift; OD-4 no commercial fonts (--dse-font-display→Obsidian font; tier badges CSS clip-path); OD-5 SDK role + grey fallback; OD-6 UNIFY stamina modals; OD-7 keep UI-state-in-note thru migration (demote to cx.session later); OD-8 validate condition color (CSS.supports) + fallback --dse-fg-muted.
Split into 2 plans: **Plan 08 = D2 FOUNDATION** (token vocabulary §6 + 10-widget accessible kit §2 + a11y default §4) — the reusable core, ADDITIVE, COEXISTS with the current kit/{componentWrapper,collapsible} (elements UNTOUCHED). **Plan 09 = per-element redesign §3 + inline-style eviction §5** (refactor the 11 elements + 5 modals onto the new kit).
Models: implementers+reviewers=Fable (design-heavy = high Fable value; Fable access ends ~07-07), final review=Opus.
Plan 08: docs/superpowers/dse-overhaul/plans/2026-07-05-plan-08-d2-foundation.md. Order: 1 tokens+DseTokenName → 2 primitives (iconButton/buttonRow, stepper, tooltip, divider) → 3 containers (collapsible2, tabs, managedModal) → 4 card grammar (cardHead, powerRollPanel/tierBadge, crest) → 5 barrel + framework focus-visible + boundary/lint.
KEY: kit ADDITIVE (new files; `collapsible2` coexists w/ old `collapsible`); ZERO color literals / ZERO el.style.color (only setProperty of --dse-* geometry: --dse-fill/-temp-fill/-value-scale/-condition-color); a11y = framework default (real <button>, aria, :focus-visible, ≥44px, REAL disabled=CB-8). D2 NAMES the ~45 --dse-* tokens + Legacy defaults (→Obsidian vars); D3 later owns values + the Steel layer. HANDOFF.md still says "migration complete, ready-to-land" — REFRESH it for D2 at the next pause.

PLAN 08 Task 1 (token vocabulary + Legacy defaults + DseTokenName union): complete (commit 8f86951, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). 62 tokens (44 core [structure10/text7/accent4/ornament9/tier4/stamina5/encounter5] + 12 roles + 6 actions). src/framework/tokens.ts = DseTokenName union (narrowed from theme.ts:15 `string`; theme.ts re-exports). styles-source.css :root BASE block — Legacy verbatim per §6 (Obsidian vars / flat-none ornament / today's literals / monochrome roles=var(--dse-fg-muted) acts=none per OD-2). Legacy=base, Steel=D3's [data-dse-theme="steel"] override (layering enforced by a test tripwire asserting NO [data-dse-theme="legacy"] block). New test/dom/kit/tokens.test.ts (8 tests, bidirectional union⊆sheet + sheet⊆union + no-dup + value pins + full monochrome sweep; @ts-expect-error tsc-gate-enforced). 534/534, tsc 0, build OK, trailer-free.
  §6 ambiguities resolved: hairline-fade=horiz gradient leg; rule-fade=transparent; heading=color-only (weight is a widget rule); tiers=var(--text-normal); roles→var(--dse-fg-muted) token-ref (a theme override of fg-muted cascades into roles).
  Minors (cosmetic, defer): trailing newline; tab-vs-space indent in the :root block; theme.ts header comment says "D3 owns the DseTokenName union" (now stale — union lives in framework/tokens.ts); stray-scan covers :root blocks only.

PLAN 08 Task 2 (kit control primitives — iconButton/buttonRow, stepper, tooltip, divider): complete (commit 8f1a5bb, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). 4 additive kit widgets + 48 tests. CB-8: real `disabled` property + in-handler synthetic-dispatch guard (tested w/ a real MouseEvent). CB-10: Enter+blur funnel into ONE change-detecting commit() (no double-fire BY CONSTRUCTION; Escape reverts dirty draft w/o closing modal; Number('') footgun handled). All listeners via owner.registerDomEvent (0 raw addEventListener; unload-teardown tested). Hygiene guard bans ALL .style + hex/rgb/named-color across the 4 kit .ts. Handle in-place updates (node-identity pinned). Consumes only Task-1 --dse-* tokens. 582/582, tsc 0, build OK, trailer-free.
  Minors (defer): (1) divider diamond punch-out uses var(--background-primary) directly (no page-bg token) — acceptable (not a literal, pixel-identical to today) but SHOULD become `--dse-page-bg` → ADD IN TASK 5 (+ repoint divider) & add to §6/D3 handoff; (2) decorative keydown/keyup in the CB-8 test; (3) Escape don't-close-modal half untested; (4) hygiene guard doesn't literal-scan the new kit CSS section (add a section-scoped scan in T5).
  Carry-forward: Task 3 collapsible = `collapsible2` (coexist w/ existing collapsible.ts until Plan 09); 44px --dse-touch-min will enlarge dense ± rows at Plan-09 migration (mandated, not a bug).

PLAN 08 Task 3 (kit containers — collapsible2, tabs, managedModal): complete (commit af96926, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). 3 additive widgets + 56 tests. collapsible2 (coexists w/ collapsible.ts): <button aria-expanded>+aria-controls; chevron rotate via [data-open] class; region via `hidden` ATTR (getAttribute('style')===null pinned + [hidden]{display:none} guard); persistKey via SessionPersist{session,blockKey,slot} accessor (cx-FREE; round-trips real createSessionStore; mount-never-writes invariant). tabs: roving tabindex (sel=0/others=-1), Arrow wrap + Home/End, aria-selected + panel-hidden, selection-follows-focus, persist+validate. managedModal: DseModal extends Modal (setDseTitle/body/footer via iconButton), openManagedModal auto-closes on owner.unload (idempotent dseOpen guard; onClose-once + footer-listener-detach tested). Hygiene guard bans .style + color-literals + elements/-imports in all 3. 638/638, tsc 0, build OK, trailer-free.
  Minors (defer): (1) tabs duplicate-id orphans a button (theoretical — add dev guard); (2) collapsible2 persisted value not type-validated (mirror tabs' validation); (3) un-titled modal has no aria-name (fine for the 5 known titled modals). ⚠️ real-Obsidian `modalEl` branch untestable in the mock → EYEBALL in-app at Plan 09 (Scott reviews rendered output anyway).
  Carry-forward Task 5: shared :focus-visible must cover .dse-collapse__header + .dse-tabs__tab (+ .dse-btn, .dse-stepper__btn, .dse-pr__row[aria-pressed]); + add --dse-page-bg token (T2 minor) & repoint divider; + section-scoped CSS literal-scan.

PLAN 08 Task 4 (kit card grammar — cardHead, powerRollPanel/tierBadge, crest): complete (commit 2d0d654, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). 3 additive widgets + 57 tests. cardHead: 6 positional slots (undefined→no element; omitted=gap pinned), name=role=heading aria-level=level??3, --left/--right/--line/--chip. powerRollPanel/tierBadge: 4 tier rows [data-tier], badge clip-paths REUSED (reviewer independently verified all 4 byte-identical to .tN-key-body; polygon-equality test pins drift), selectable=role=radiogroup of real <button aria-pressed> w/ roving arrow-nav anchored on the event-target row (works pre-selection), single change() funnel {notify,focus}, renderMd(md,el) callback keeps kit app-free (NO MarkdownRenderer/app import; guard-enforced), color-not-sole-signal (range text always present). crest: Steel-only, hidden everywhere for now (active theme='steel' pre-D3 → showing would flash unskinned glyphs; test pins + documents D3 relaxation), null w/o icon. 695/695, tsc 0, build OK, trailer-free.
  *** Carry-forward TASK 5 (token gaps): add `--dse-page-bg` (T2 — value var(--background-primary); repoint divider diamond) + `--dse-badge-fg` (T4 — currently `var(--dse-badge-fg, var(--dse-fg))` fallback) to tokens.ts union + :root base; bump tokens.test count 62→64. ***
  Minors (defer): (1) powerRollPanel focus no-op when arrowing onto the already-selected row after a programmatic desync (edge); (2) cardHead empty-string '' mounts an empty chip (treat ''=omitted or doc); (3) powerRollPanel clip-path test indexOf-anchoring order-fragile (strip comments like crest.test does).

PLAN 08 Task 5 (kit barrel + framework :focus-visible + token gaps + literal-scan + boundary): complete (commit ef6ca43, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). index.ts barrel (all 15 runtime exports + 11 Handle types; reviewer verified genuinely complete — kit = 12 modules + index). Framework :focus-visible §4.5 = ONE shared rule over .dse-btn/.dse-stepper__btn/-input/.dse-collapse__header/.dse-tabs__tab/.dse-pr__row[aria-pressed] (Task-4 standalone row-ring CONSOLIDATED; exactly-one pinned; [aria-pressed] narrowing correct — only selectable rows are buttons w/ aria-pressed at mount). Token gaps closed: --dse-page-bg=var(--background-primary), --dse-badge-fg=var(--dse-fg); count 62→64; divider + powerRollPanel repointed (value-identical, pinned). Section-scoped kit-CSS literal-scan (7 inject-fail + 6 pass fixtures; strips comments; markers throw if moved). Whole kit ZERO color-literals + ZERO el.style.color + boundary clean (jest-enforced; eslint zone rule still inert per F3). 745/745, tsc 0, build OK, trailer-free. (No `@framework` alias → barrel imports as @/framework/kit.)
  Minors (defer/hardening): (1) literal-scan regex misses hwb/lab/lch/oklch/color() — widen; (2) exactly-one-ring pin format-sensitive; (3) boundary regex misses dynamic import()/require; (4) --dse-badge-fg resolves at :root not the badge → D3-forward note (the Steel layer must set badge-fg explicitly, won't inherit a scoped --dse-fg override).

################################################################
=== PLAN 08 (D2 FOUNDATION) — ALL 5 TASKS COMPLETE, all reviews clean (implementers+reviewers=Fable). ===
Token vocabulary (64 --dse-* tokens, Legacy=:root base; DseTokenName union) + the 10-widget accessible kit (iconButton/buttonRow, stepper, tooltip, divider, collapsible2, tabs, managedModal/DseModal, cardHead, powerRollPanel/tierBadge, crest) + the framework a11y default (:focus-visible, real disabled=CB-8, no-double-commit=CB-10, roving tabindex, aria, ≥44px, hidden-attr, owner-bound listeners). ADDITIVE — elements + existing kit helpers UNTOUCHED (coexist until Plan 09). ZERO color literals / ZERO el.style.color across the kit (guard-enforced). Branch dse-framework, increment e40ad2c..ef6ca43 (5 commits: 8f86951 tokens, 8f1a5bb primitives, af96926 containers, 2d0d654 card-grammar, ef6ca43 tie-off). 745 tests / tsc 0 / build OK, all trailer-free.
NEXT: Opus final whole-increment review (e40ad2c..ef6ca43), then Plan 09 = per-element redesign (D2 §3) + the 65-site inline-style eviction (§5) — refactor the 11 elements + 5 modals onto this kit. (Also refresh HANDOFF.md for D2.)
################################################################

=== PLAN 08 FINAL whole-increment review (opus): READY TO LAND = YES ===
No Critical, no must-fix. Opus: "one coherent, disciplined foundation, not five separately-built widgets" — uniform (parent,opts,owner)→Handle factory, a11y-by-construction genuinely tested (mock does real add/removeEventListener + jsdom real disabled/focus, so CB-8/CB-10/roving-tabindex/hidden-attr/owner-unload-detach are REAL DOM tests), token vocab complete + bidirectionally pinned + sound base/Steel layering, boundary (kit⊥elements, zero literals, renderMd/SessionPersist decoupling) holds. SOUND FOUNDATION FOR PLAN 09 = YES (widget set maps directly onto the named consumers).
=== *** D2 FOUNDATION COMPLETE (Opus-confirmed): 64-token vocabulary + 10-widget accessible kit + a11y default. Additive; 745 tests; ready to land. *** ===
IMPORTANT (fast-follow, NOT a landing blocker — nothing consumes it yet): powerRollPanel selectable = role="radiogroup" + <button aria-pressed> children = ARIA MISMATCH (radiogroup wants role="radio"+aria-checked). Spec §2.8 itself prescribes the button+pressed mix. Keyboard mechanics already ARE the radio pattern (correct); only child role/state diverges. DECISION (orchestrator, 2026-07-05): fix to a TRUE radiogroup — rows role="radio"+aria-checked — honors the spec's "resolves to exactly one tier" intent. → PLAN 09 KICKOFF TASK.
=== PLAN 09 KICKOFF fast-follows (do FIRST, before element refactors — Opus-recommended): (1) powerRollPanel radiogroup→role=radio+aria-checked; (2) MOVE `SessionPersist` off collapsible2.ts to a neutral module (framework/session.ts or kit/persist.ts) so the collapsible2 rename doesn't break tabs' import + the barrel; (3) RECONCILE the per-file kit `.style` guards (they ban ALL .style) to the tsColorFindings shape (ALLOW setProperty('--dse-*',…) geometry) BEFORE a geometry widget (stamina-bar width) lands. ===
Opus Minors (defer): literal-regex misses hwb/lab/lch/oklch/color(); one-ring pin format-sensitive; boundary regex misses dynamic import(); cardHead name='' mounts empty heading; tabs dup-id orphans; stepper editable aria-live ineffective (native spinbutton covers it); managedModal real-Obsidian modalEl branch + Escape-close untested → IN-APP EYEBALL at Plan 09. NOTE for Plan 09 authors: --dse-role/--dse-act are ELEMENT-SET aliases (data-dse-role="x" → --dse-role: var(--dse-role-x)); elements must wire that mapping or the accent fails safe to monochrome (intended Legacy).

################################################################
=== PLAN 08 COMPLETE & CLEAN @ ef6ca43 (increment e40ad2c..ef6ca43, 5 commits, 745 tests / tsc 0 / build OK, trailer-free). Opus: ready-to-land=YES. ===
=== D2 STATUS: FOUNDATION done (Plan 08 = tokens + kit + a11y). NEXT = Plan 09 (element redesign §3 + inline-style eviction §5) — refactor 11 elements + 5 modals onto the kit. Then D3 (theme values/Steel/Legacy sheet) + D4 (prefs). ===
=== FULL dse-framework BRANCH: b80a8a9 (pre-work) → ef6ca43. HANDOFF.md refreshed for D2. ===
################################################################

################################################################
PLAN 09 — D2 per-element redesign onto the kit + inline-style eviction. WRITTEN + ready to execute.
Plan: docs/superpowers/dse-overhaul/plans/2026-07-05-plan-09-d2-element-redesign.md (11 tasks).
Order: T0 kickoff (radiogroup fix + SessionPersist relocation + .style-guard reconcile) → T1 HR+ValuesRow+Characteristics (→.dse-statgrid) → T2 Skills (collapsible groups) → T3 Stamina bar + unify the 2 stamina modals (SC-5 epicenter, OD-6) → T4 Counter (stepper) → T5 Feature (cardHead+powerRollPanel+sections+act-spine; DEFINES the shared feature grammar) → T6 Featureblock+Statblock (reuse Feature; role tint; statblock highest-value) → T7 Negotiation (tabs+selectable-radiogroup+bubbles; CB-4/CB-16) → T8 shared modals (Condition/Customize/Reset→managedModal) → T9 Initiative (a11y epicenter; CB-7/CB-6; migrates last) → T10 cleanup (delete old collapsible/componentWrapper, rename collapsible2→collapsible).
KEY: DOM+CSS ONLY — NOT content/persistence (persisted elements keep byte-identical serialize; replace the migration golden-DOM tests w/ kit-DOM+a11y; keep persist byte-compat tests). Legacy=today's look via tokens; D3 adds Steel. Controls from the kit (zero hand-rolled divs, zero raw addEventListener, real disabled=CB-8); zero color literals / zero el.style.color (only setProperty --dse-* geometry). --dse-role/--dse-act = ELEMENT-SET aliases (data-dse-role="x" → --dse-role: var(--dse-role-x)). Manual in-app visual-QA = the look-acceptance gate (Scott reviews rendered output).
Models: implementers+reviewers=Fable (while access lasts ~07-07; then Sonnet/Opus), final=Opus. BASE for T0: ef6ca43.
PLAN 09 Task 0 (kickoff — radiogroup ARIA + SessionPersist relocation + .style-guard reconcile): complete (commit 312efd8, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable; ATTEMPT-1 hit usage limit w/ NO commit, retry post-reset succeeded). powerRollPanel selectable rows = <button role="radio" aria-checked> (was aria-pressed; machinery untouched — arrow/Home/End/roving/one-checked intact); CSS [aria-pressed]→[aria-checked] in BOTH focus-visible (:2949) + selected-paint (:2842); FOCUSABLE_KIT_CONTROLS pin updated. SessionPersist moved to framework/session.ts:42 (collapsible2/tabs/barrel repointed). Shared test/dom/kit/styleGuard.ts allows setProperty('--dse-*',…) geometry, still bans .style.color/color-props/other-.style + scans setProperty VALUES for hex (blanks only the call-head); kit-index CSS scan widened to hwb/lab/lch/oklab/oklch/color(). 753/753, tsc 0, build OK, trailer-free. NO src/elements touched.
  Minors (defer, styleGuard.ts polish): color-fn regex lacks /i; named-color list abbreviated vs kit-index full set; //-comment strip can eat string content (false-neg edge); color( false-positive on a helper. ⚠️ kit-index tsColorFindings NOT widened to modern color-fns (only the CSS scan was — coverage asymmetry, not a defect).
=== P09 kit HARDENED; element refactors unblocked. ===

PLAN 09 Task 1 (trivial static — HR + Values Row + Characteristics onto the kit): complete (commit 019c347, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). HR view → kit divider(root,{axis:h,ornament:true}); legacy horizontalRuleProcessor.ts UNTOUCHED + guarded by a tripwire test (Task 6 deletes it when statblock/featureblock migrate off .build()). ValuesRow+Characteristics unified onto ONE .dse-statgrid grammar ([data-dse-element] distinguishes; __cell/__value/__label); value_height/name_height → setProperty('--dse-value-scale'/'--dse-label-scale',…) [--dse-* geometry; defaults 3em/1em match legacy]; @media column-flip kept as a TOP-LEVEL media rule (esbuild &{}-in-nested-@media footgun avoided + regex-guarded). ValuesRowView+CharacteristicsView folded in + DELETED (1:1 port; only comments remain). Old .ds-values-row-*/.ds-characteristics-* CSS evicted (negative-assertion pinned). RegisterElements/plugin-wiring = comment/assertion-swap only. Zero literals / zero el.style.color (styleGuard-enforced). 762/762, tsc 0, build OK, trailer-free. Static.
  Minors (defer): (1) __value gains explicit color:var(--dse-fg) (=var(--text-normal) in Legacy, identical in normal view; forces normal inside a colored callout — intended D2 grammar/D3 hook); (2) overflow-x moved to the single .dse-statgrid (equivalent; narrow-width visual spot-check). --dse-label-scale = new geometry prop (D2 §5 doc-addendum candidate). Task-6 carry-forward: delete legacy HR builder + .ds-hr-* CSS + the guard test.

PLAN 09 Task 2 (Skills → kit collapsible groups): complete (commit e90c1f0, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). collapsible2 whole-element (slot 'open', honors collapsible/collapse_default; session-beats-seed precedence verified) + per-group (group:<key>) via SessionPersist (remount-tested w/ real createSessionStore). Marks = <span role=img>[data-on] + aria-label enabled/disabled (shape+label, NOT color). Owner simplification (content mounts once; kit hides via hidden attr — removes the old per-cycle contentOwner listener-stacking). only_show_selected/collapsible/collapse_default preserved; ZERO vault writes; ZERO color literals (styleGuard); old .ds-skill* CSS evicted (negative-pinned); old kit helpers UNTOUCHED. 770/770, tsc 0, build OK, trailer-free. Only Skills touched.
  *** VISUAL-QA (Scott — Legacy look-CHANGE riding the a11y redesign): Skills whole-element collapse toggle = hover-EYE → a visible titled 'Skill List' header button (spec-directed per §2.3 = collapsible replaces ComponentWrapper+eye; title='Skill List'=old componentName). Reviewer: appropriate usage + sensible title, but EYEBALL the stacked-chrome look (whole-element header directly above 6 group headers = a lot of collapse chrome for a small element; §3.4's diagram shows only group headers). Cheap remedies if noisy: quieter default title style OR drop the whole-element wrapper title (kit-config, no rework). ***
  Minors (defer): (a) collapsible:false+collapse_default:true edge now renders content (an IMPROVEMENT — old=permanently-collapsed-unreachable; ACCEPT) but UNTESTED → add a 1-line pin; (b) mountCollapsibleHeading is now element-DEAD (only stamina-bar still uses mountComponentWrapper; kit/collapsible.ts fully element-dead) → Task 10 can delete kit/collapsible.ts once negotiation(T7) moves off too; (c) collapsible2 emits a bare <button> not <h3><button> (APG disclosure wants the heading wrapper) — kit-level FOLLOWUPS candidate, not a Skills bug.

=== P09 PROGRESS: T0 (kickoff) ✓ · T1 (HR/ValuesRow/Characteristics) ✓ · T2 (Skills) ✓ — 3/11 done. HEAD e90c1f0, 770/770, tsc 0. NEXT = T3 (Stamina bar + unify the 2 stamina modals — SC-5 epicenter, OD-6). BASE for T3 = e90c1f0. ===
=== ACCUMULATING VISUAL-QA (Scott reviews rendered output): (T1) statgrid overflow-x moved to the single .dse-statgrid → eyeball narrow-pane scroll; statgrid __value gains explicit color:var(--dse-fg) (identical in normal view, differs inside a colored callout). (T2) Skills 'Skill List' whole-element header. (T3) stamina 'Stamina Bar' header (was hover-eye); bar temp-fill purple→deepskyblue; Kill=red danger variant; modal 3-section layout; checked-minion condition-icons tinted crimson. ===
################################################################

PLAN 09 Task 3 (Stamina bar + unify the 2 stamina modals — SC-5 epicenter · OD-6): implementer COMMITTED cc045ad then hit the usage limit (4th; reset 9:20pm ET) BEFORE self-reporting. ORCHESTRATOR-VERIFIED green: trailer clean; only stamina-bar/view.ts + StaminaEditModal + MinionStaminaPoolModal + CSS + tests touched (no other element/kit); tsc 0; 791/791 (was 770, +21); NO el.style.color/color-literal in code (grep hits = comments only). Per report: 43 .style sites evicted → 8 sanctioned setProperty(--dse-*); bar = .dse-stamina + [data-state] + --dse-fill/--dse-temp-fill; wrapper mountComponentWrapper→collapsible2 ('Stamina Bar', seeded-open, NO persist — remount-fresh tested); ONE unified DseModal template + optional .dse-sedit__minions (shared primitives in StaminaEditModal.ts); all controls kit/CB-8 real disabled; CB-1/CB-2/DC-6 nets green verbatim; persist BYTE-IDENTICAL claim (byte-exact YAML via real ReadingModeBlockHost+FakeVault; serialize/model untouched). BASE e90c1f0.
  REVIEW (Fable): redesign/unification/eviction/CB-1/CB-2/DC-6-nets EXCELLENT + well-netted, BUT byte-compat gate flagged 2 reachable divergences:
   [CRIT #1 — FIXING] kit stepper commits Number(raw) w/ no int coercion → typing 7.5 persists a FLOAT where legacy parseInt'd 7 (stamina/temp/pool steppers; would recur in Counter T4). FIX = add `integer` opt to the kit stepper (Math.trunc typed commit) + staminaStepperRow sets it.
   [IMP #2/#3 — *** SCOTT'S DECISION ***] the unbounded single-stamina stepper diverges from legacy's CORRECTIVE ± snap in the degenerate OVERSHOOT corridor (max20/cur10 → SpendRecovery×2 → + → −×3 → Apply: NEW persists 20, LEGACY 17); + the pool 'type 25 into a full 20-pool' corridor (legacy materialized pool:20; new = no write). NO corruption; new value arguably cleaner; but a reachable persisted-value diff vs the "byte-identical persistence" mandate → present to Scott: reproduce legacy's bug-shaped ± step (needs a custom stepper ± hook) VS accept the cleaner behavior. Code currently HAS the deviation; FIX PASS only corrects the now-false in-code comment (± behavior UNCHANGED pending Scott).
   [MINOR] cursor:pointer dropped on condition-remove icons (FIXING); pill bg raw var(--background-primary-alt) + hatch #000 ink (legacy-parity, D3 token note); shared primitives live in StaminaEditModal.ts (T8 may re-lift to a shared views module).
  FIX PASS COMPLETE (b5b2963; Fable): stepper `integer` opt (Math.trunc typed commit, default-OFF — Counter T4 reuses it), cursor restored, comment corrected (± UNCHANGED). Orch-verified: trailer clean, scoped to stepper+2 modals+CSS+tests, tsc 0, 797/797 (+6).
=== TASK 3 COMPLETE (review clean post-fix; implementer+reviewer+fixer=Fable). *** #2/#3 = SCOTT'S PENDING byte-compat decision (overshoot ± corridor + pool type-past-max): code currently HAS the cleaner-behavior deviation; reproduce legacy's bug-shaped ± step OR accept. *** HEAD b5b2963, pushed origin/dse-framework. ===

PLAN 09 Task 4 (Counter → kit stepper): complete (commit ac36e77, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). Manual chevrons+edit → kit stepper (integer:true [typed 5.5→5, byte-verified], editable:canPersist [CB-10 Enter-once/Escape-revert], real disabled at min/max [CB-8]); onChange → mutate model → EXACTLY-ONE debounced byte-identical persist (real ReadingModeBlockHost+FakeVault, ~~~ds-ct fence preserved). value_height/name_height → --dse-value-scale/--dse-label-scale setProperty; input height → CSS class; ZERO inline font-size/color. canPersist read-only preserved (static span, force-disabled real property, data-dse-readonly, zero writes — triple-gated). Old .ds-counter* CSS evicted. 801/801, tsc 0, build OK, trailer-free. Only Counter touched.
  *** SCOTT-PENDING byte-compat decision #2 (adds to T3's): OUT-OF-RANGE persisted values — the kit stepper INIT-CLAMPS, so current_value:25/max:20 DISPLAYS 20 (legacy 25) + first − persists 19 (legacy 24); current_value:-5 → 0. NO unprompted write, but SILENT misrepresentation of stored state (grazes Scott's explicit-state preference). Only reachable via hand-edited/out-of-range YAML. Reviewer: FOLLOWUPS (kit `clampInitial:false` opt) or a pinned-test+comment; NOT a T4 rework. → Scott decides clamp-display vs show-as-stored. ***
  Minors → FOLLOWUPS: (#2) integer path uses Number()→NaN on '12abc' (legacy parseInt→12); switching the integer typed-commit to parseInt(raw,10) matches legacy exactly + is strictly better — cheap kit tweak (also closes this for stamina T3); (#3) silent optional-chain on the value-class tag (test-caught); (#4) read-only 'no listeners'→'listeners inert but force-disabled' (behaviorally equivalent, zero-writes pinned).
  VISUAL-QA (Scott): Counter vertical chevrons → horizontal [−] value [+] stepper; kit button chrome vs bare chevrons.
=== TASK 4 COMPLETE. HEAD ac36e77 → pushed. ===

PLAN 09 Task 5 (Feature → reusable renderFeature card grammar): complete (commit 06caf3a, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). NEW reusable `renderFeature(parent, config, owner, renderMd, opts?)` + `renderFeatureList` + `actionTypeOf` in src/elements/feature/renderFeature.ts (callback-based, NO MarkdownRenderer/app import — ML-1 pinned: spy asserts the FeatureElementView component + sourcePath on every render). Emits .dse-feature: cardHead (name→heading, cost→rightEyebrow, ability_type→rightPrimary) + .dse-feature__meta + __flavor + STATIC .dse-pr (4 tiers, VERBATIM roll head — never the kit's invented 'Power Roll' caption) + .dse-section (trigger-before-effects) + [data-dse-act] spine (element-set --dse-act alias, fails-safe unmapped, Steel-only). Nesting/indent-N + effect.features recursion preserved. Reviewer verified fidelity LINE-BY-LINE vs legacy FeatureView+EffectView: dash-quirk ('-'→'--') scoped EXACTLY (FeatureView fields only, not effect bodies); 19-string no-loss pin. Legacy Features/FeatureView.ts + .ds-feature-* CSS BYTE-UNTOUCHED (statblock/featureblock still use them — pinned both ways). Zero color literals; static. 822/822, tsc 0, build OK, trailer-free. Only Feature touched.
  Fidelity deltas adjudicated: (a) cost chip drops legacy parens + moves inline→right-slot — SPEC-SANCTIONED (§3.6 mockup) but the ONE visible Legacy change → Scott visual sign-off (Legacy CSS ::before/::after could restore parens if wanted); (b) meta labels Legacy-hidden = EXACTLY FAITHFUL (legacy shows NO labels today — empty spans; new keeps display:none + pre-wires D3).
  Minors: (#2 → FOLD INTO T6) .dse-pr__head reach-in (renderFeature.ts:199) — powerRollPanel exposes no head hook; T6 statblocks hit headless-tiers commonly → add `head?: string|false` opt (or headEl to the Handle) to the kit powerRollPanel + repoint renderFeature. (#3) Trigger:/Effect: now ::after content → excluded from copy-paste/find-in-page (tiny; enables Steel colon-drop). (#4) nameless-feature → empty role=heading (guard only if a real fixture hits it).
  VISUAL-QA (Scott): Feature cost chip position/parens (§3.6 head re-layout).
=== TASK 5 COMPLETE. HEAD 06caf3a → pushed. ===

PLAN 09 Task 6a (powerRollPanel head opt + Featureblock → kit card grammar): complete (commit a792dfb, review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). [T6 SPLIT into 6a=featureblock / 6b=statblock — each independently reviewable given size.] powerRollPanel `head?: string|false` (string=verbatim head via renderMd; false=NO head element + selectable aria-labelledby degrades gracefully [pinned]; undefined=default 'Power Roll + chars'); headEl? on the Handle. renderFeature REPOINTED to head: effect.roll?.trim()||false — the .dse-pr__head querySelector reach-in REMOVED (hygiene test pins renderFeature querySelector-free); behavior-preserving. Featureblock → cardHead (kind-noun/name/Level/role/EV fill) + data-dse-role alias (fails-safe monochrome) + .dse-fb__flavor/__stats([data-dse-fb-stats])/__band--adv + renderFeatureList (T5). Reviewer verified field coverage LINE-BY-LINE vs FeatureblockView+FeatureblockStatsView (level/ev/flavor/stamina/size/stats[]/features[]; level:0 identical; 2 no-loss sweeps). Legacy FeatureblockView+sub-views BYTE-UNTOUCHED; .ds-fb-* evicted; zero color literals; static; ML-1 owner-parented. 842/842, tsc 0, build OK, trailer-free.
  Minors → FOLLOWUPS: (#1) head:'' mounts an empty head (unreachable — caller normalizes; normalize ''→false in the kit or docstring); (#2) advancement band label lacks heading semantics (site-parity; role/aria); (#3) fbRoleOf is vocabulary-order not word-order (theoretical; swap for real SDK kind/role fields) + role-carrying type leaves left-eyebrow gap (single-field trade-off).
  T10 carry-forward: T6b is the LAST consumer of legacy HeaderView/FeaturesView/HR builders + shared .ds-* CSS → after T6b retire them (incl. the legacy HR builder + .ds-hr-* + the T1 guard test).
=== TASK 6a COMPLETE. HEAD a792dfb → pushed. ===

PLAN 09 Task 6b (Statblock → kit card grammar): implementer COMMITTED 3e52489; ORCHESTRATOR-VERIFIED green (tsc 0, 854/854 [was 842, +12], trailer clean, no color-literal/el.style.color in statblock+roleTint code, legacy builders untouched, pushed origin). Statblock → cardHead (§3.8: ancestry/keywords→left-eyebrow, name→left-primary, Level→right-eyebrow, Org·Role→right-primary, EV→right-deck) + data-dse-role spine (--dse-role alias, fails-safe; org words Boss/Horde unmapped→monochrome) + .dse-sb__meta (Immunity/Weakness, Movement/captain, Size/Speed/Stamina/Stability/Free Strike — VERBATIM) + .dse-sb__chars (5 chars via ported formatCharacteristic) + renderFeatureList + [data-dse-density]/[data-dse-sb-featstyle] attrs. NO word/number change (community constraint). Extracted src/elements/roleTint.ts (shared, from T6a's fbRoleOf; featureblock import-swapped) + updated 1 stale assertion each in feature/featureblock tests. Legacy HeaderView/StatsView/FeaturesView/HR now ELEMENT-DEAD (T10 retires). REVIEW CLEAN (Spec ✅ / quality Approved; implementer+reviewer=Fable; 1st review attempt bounced on usage limit @0 tokens, retried post-4:30am-reset). Reviewer verified verbatim fidelity CHAR-FOR-CHAR vs legacy HeaderView+StatsView incl. the `?? 0>0` precedence quirk + the empty-captain div; roleTint extraction byte-identical; the 2 test edits net-STRONGER (widened to ban the whole legacy tree, nothing masked); +30 behavior tests w/ a new WITH_META fixture. BASE a792dfb.
  Minors → FOLLOWUPS: (#1) roleTint.ts .style write (the --dse-* hatch) isn't covered by any styleGuardFindings scan — add it + a roleOf unit suite (cheap); (#2) CSS-owned colon changes clipboard (copies 'Immunity-' not 'Immunity: -') — statblock most-copied → visual-QA.
  VISUAL-QA (Scott): highest-value element — header now kit chip grammar (cardHead re-layout, like Feature); Legacy parity token-level → eyeball a real statblock in both themes.
=== TASK 6b COMPLETE (all of T6 done). Branch pushed through 3e52489. Usage limit hit 5× this session (all recovered post-reset). ===

PLAN 09 Task 7 (Negotiation → kit tabs/radiogroup/bubbles): implementer COMMITTED 7e73bb1; review-in-progress. [CORRECTION: negotiation NEVER used mountComponentWrapper (only a comment) → componentWrapper.ts was already ELEMENT-DEAD after T3, not T7. BOTH old kit helpers (componentWrapper.ts + kit/collapsible.ts) are element-dead → T10 deletes.] 4 sub-views rebuilt (PatienceInterestView/ArgumentView/LearnMoreView/MotivationsPitfallsView). Name→cardHead (CB-16: no dangling 'Negotiation: '; unnamed→plain 'Negotiation'). Patience/Interest bubbles = iconButton aria-pressed (44px, labelled, keyboard); two-tab → kit tablist (roving, session slot 'tab'); argument tiers = powerRollPanel(selectable) role=radio/aria-checked (renderMd→this.renderMarkdown); motivations = .dse-nt__motivations. CB-4 reset per-instance (two-tracker independence pinned). Read-only = real-disabled + menu/Complete omitted. Byte-compat CONFIRMED (model/serialize untouched; every mutation byte-identical vs stringifyYaml oracle, exactly-one debounced write, render writes ZERO — legacy's persist-during-render dropped). Old .ds-nt-* evicted; zero color literals. 867/867 (+13), tsc 0, build OK, trailer-free. BASE 3e52489.
  Concern → SCOTT/visual: radiogroup tiers DROP legacy tier DESELECTION (can't toggle a tier off — a roll resolves to exactly one tier per the T0 decision) + the checked radio stays painted until the post-Complete echo-rebuild (intended per T0, but a visible interaction change). Ties to the T0 radiogroup-vs-segmented decision.
  VISUAL-QA (Scott): bubbles 44px vs ~32px; pressed = kit accent fill. Minor: cardHead CSS-contract test greps the sheet's first `.dse-head {` — element CSS must not spell that selector (test-fragility note).
=== TASK 7 COMPLETE (review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). Byte-compat oracle coverage 'exemplary'; CB-4 two-instance test pins the REAL legacy singleton trap; a11y controls kit-source-verified; model/serialize GIT-confirmed untouched. Radiogroup deselection-drop = ACCEPTABLE-PER-DESIGN (no data risk — selection never writes; matches legacy .active linger) → relay to Scott as a known interaction change. Minors→FOLLOWUPS/visual-QA: post-Complete same-tier ~400ms dead-window (inherent — prevents acting on stale numbers); disabled-checkbox hover chrome (visual-QA); .dse-nt__head `> :first-child` positional coupling (later: anchor the kit cardHead-contract grep). 8/11 P09 DONE. → pushed 7e73bb1. ===

PLAN 09 Task 8 (Condition/Customize/Reset modals → managedModal + validated condition color): implementer COMMITTED 57a3130; review-in-progress. All 3 modals → DseModal/managedModal (aria-labelledby, iconButton footer CB-8, auto-close). NEW src/elements/conditionColor.ts: applyConditionColor(el, color) validates via CSS.supports('color',v) → valid sets --dse-condition-color, invalid/absent clears (CSS fallback --dse-fg-muted); NEVER el.style.color (OD-8/SD-2). Also applyConditionEffect + CONDITION_EFFECTS. ConditionSelectModal rows = .dse-cond-item[aria-selected]; cog reveals :hover AND :focus-within. CustomizeConditionModal keeps native <input type=color>; validated preview. ResetEncounterModal = managedModal confirm. Constructor signatures + callbacks BYTE-IDENTICAL for T9 (initiative SOURCE untouched — only 3 initiative.test.ts selector updates to the new labelled-button modals). test/setup/dom-setup.ts += a CSS.supports shim (jsdom). 915/915 (+48 across 4 new test files), tsc 0, build OK, trailer-free. BASE 7e73bb1.
  T9 MUST adopt applyConditionColor(el,color): use at initiative/view.ts:565; T3's MinionStaminaPoolModal.ts:390 (currently unvalidated) should adopt too (T9 or FOLLOWUPS). Concern: reduced-motion disabling of .condition-effect-* left for T9. Exemption: <input type=color> #ffffff default (form data).
=== TASK 8 COMPLETE (review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable). Reviewer RAN the focused validation suite: 15/15 incl. injection rejections ('; }', expression(alert(1)), url(javascript:...)) — the CSS.supports gate is genuine + STRUCTURALLY non-vacuous (invalid-input tests assert what LANDED on the element, shim-independent); cog keyboard-reachable (visibility:hidden + :focus-within); T9 signatures BYTE-IDENTICAL (initiative source untouched; test edits = stronger labelled selectors); lifecycle/nested-close tested; CSS eviction complete. Minors→FOLLOWUPS: aria-selected on a role-less div (spec-mandated hook; SR uses aria-pressed — fine; strict-ARIA later = role=option/data-attr); dom-setup CSS.supports shim stricter than a real browser on modern color syntax (test-only, fails-closed); conditionColor.ts:57 cosmetic re-check. → push 57a3130.
REMAINING: T9 Initiative (a11y epicenter — biggest/last element: every click-div → real control; CB-7 Malice stepper [no setText chevron-wipe], CB-6 cell in-place refresh; ADOPT applyConditionColor at view.ts:565; data-dse-portraits pref; reuse T8 modals) → T10 cleanup (delete legacy FeatureView/FeaturesView/FeatureblockView/HeaderView/StatsView/HR builders + componentWrapper.ts + kit/collapsible.ts + .ds-* CSS + T1 HR guard; rename collapsible2→collapsible) → Plan 09 Opus final review. BASE for T9 = 57a3130. ===

PLAN 09 Task 9 (Initiative → kit, a11y epicenter): implementer COMMITTED 92de510; review-in-progress. Every click-div → real kit control. Turn = iconButton(aria-pressed/[data-taken]); Malice = kit stepper (CB-7: value in-place, ± buttons survive, integer); cells = <button>(aria-pressed/[data-selected] via --dse-select, CB-6 data-instance-key targeted refresh — troll-vs-nth-child(1) tested); stamina = [data-state]→--dse-stamina-*/--dse-danger (inline red/green EVICTED); conditions via validated applyConditionColor/applyConditionEffect (T8) + reduced-motion @media; actionbar = buttonRow. ≥44px via ::after overlay. Read-only = ZERO buttons/writes. Byte-compat CONFIRMED (model/serialize/resolveRefs untouched; every mutation exactly-one byte-identical persist vs oracle + real host+FakeVault). data-dse-portraits reflection + CSS. Old .ds-init-*/.malice-* evicted; styleGuard pins zero inline color. 923/923 (+8), tsc 0, build OK, trailer-free. BASE 57a3130.
  Notes/carry: (a) Malice triad → horizontal −/＋ stepper (visual delta, spec-sanctioned) → VISUAL-QA; (b) temp-stamina green→limegreen token convergence; (c) no VP field in the model → .dse-init__vp/--dse-vp RESERVED (spec diagram showed VP, model lacks it); (d) D4 must register the portraits descriptor; (e) T3 MinionStaminaPoolModal.ts:390 condition color STILL unvalidated (out of T9 scope) → FOLLOWUPS: adopt applyConditionColor there.
=== TASK 9 COMPLETE (review clean — Spec ✅ / quality Approved; implementer+reviewer=Fable; reviewer re-ran the focused suite 33/33). Byte-compat verified from EVERY angle (oracle re-pins + real host + read-only ZERO-writes + model git-untouched); CB-7 fixed BY CONSTRUCTION (stepper touches only its value node; ± buttons never rebuilt); CB-6 tested on the EXACT legacy failure (troll key 1-1 vs nth-child(1): right cell updated + victim unchanged); read-only = literally 0 <button>s; token parity exact. Deliberate/disclosed deviations: VP reserved (no model field — byte-compat forbids adding), 24×44 condition hit-targets (WCAG 2.5.8 AA — avoids overlapping zones). Both correct.
  Minors: (#1 → FOLD INTO T10) .dse-cond cursor:pointer hits read-only static spans → scope to `button.dse-cond` (Scott's explicit-read-only principle); (#2) integer:true inert on the non-editable malice stepper (future-proof; add a comment); (#3) detail-row listener accumulation = pre-existing Plan-06 pattern (bounded, unload-released — NOT a T9 regression).
=== ✅✅ ALL 11 ELEMENTS + 5 MODALS REDESIGNED onto the kit + each reviewed clean. → pushed 92de510. ===

PLAN 09 Task 10 (CLEANUP): implementer COMMITTED 10e9d11; review-in-progress. collapsible2→collapsible RENAME complete (old mountCollapsibleHeading collapsible.ts DELETED, D2 widget renamed in; grep collapsible2 → NOTHING anywhere). DELETED (all provably dead, tsc 0 after each batch, NONE left undeleted — no live consumers): kit collapsible.ts(old)+componentWrapper.ts + Features/{FeatureView,FeaturesView,EffectView} + featureblock/{FeatureblockView,FeatureblockStatsView} + statblock/StatsView + Common/{HeaderView,horizontalRuleProcessor,BoldKeyWithValueView} + their orphaned tests. CSS evicted: .ds-feature-*/.ds-fb-*/.ds-sb-*/.ds-hr-*/.ds-header-*/.ds-bkv-*/tier-key boxes (orch-grep confirmed NO live element renders them); badge clip-path polygons now pinned LITERALLY in the kit test (legacy .tN-key-body gone). Minor#1 cursor→button.dse-cond + malice integer comment applied. RegisterElements/statblock-definition = dead-import removals. 900/900 (was 923, −23 dead-code tests), tsc 0, build CLEAN, trailer-free. Net −1593 lines. BASE 92de510.
  Leftovers (FOLLOWUPS): import-dead CodeBlocks.ts optional sweep; uncalled labeledIcon.
PLAN 09 Task 10 review: CLEAN (Spec ✅ / quality Approved; implementer+reviewer=Fable). Reviewer INDEPENDENTLY re-verified in the worktree: rename token-complete (D2 widget logic byte-untouched; SessionPersist resolves from session.ts); every deletion provably dead (0 live importers — drawSteelAdmonition now holds only EncounterData + the 4 live negotiation sub-views); CSS eviction removed ONLY unrendered classes (only surviving legacy .ds- = .ds-labeled-icon, live-emitted); badge polygon pin byte-identical + STRONGER (literals vs the old drift-together selector compare); Minor#1 behavior-correct (write path = iconButton.buttonEl button keeps pointer, read-only path = createSpan loses it); no collateral (RegisterElements/definition hunks comment-only). Focused re-run 60/60. Minors (trivial): report arithmetic loose (not wrong); harmless collapsible.ts double-coverage.
=== ✅✅✅ PLAN 09 COMPLETE — all 12 sub-tasks done (T0 kickoff + T1–T10), each reviewed clean by Fable. ALL 11 ELEMENTS + 5 MODALS on the D2 kit; legacy builders + old kit helpers GONE; the kit is the single control/container source. Pushed 10e9d11. ===

=== PLAN 09 OPUS FINAL REVIEW (ef6ca43..10e9d11): READY TO LAND = YES. ===
No Critical. Cross-cutting: coherence STRONG (kit consumed w/ ONE vocabulary; renderFeature/roleTint/conditionColor GENUINELY single-sourced; fails-safe alias pattern identical in renderFeature+roleTint); byte-compat UNIFORM (same mutate→debounced-persist idiom in all 4 persisted elements; model/serialize untouched; oracle-pinned; read-only inert); a11y CONSISTENT (ZERO surviving click-divs — only raw addEventListener is SettingsTab, outside scope; controls exclusively from the kit); SC-5 COMPLETE (grep-clean tree-wide — the only literal = the <input type=color> #ffffff form-default; styleGuard covers 20+ element/modal sources). Cleanup real (−1593 lines). SOUND FOUNDATION FOR D3+D4 = YES (token vocab complete + forward-reserved incl. --dse-vp; base/Steel layering structured; elements emit --dse-* + data-dse-{role,act,density,sb-featstyle,portraits,readonly,state,selected,taken} uniformly via shared helpers).
IMPORTANT (Should-Fix, NOT a landing blocker) — FIXING NOW: MinionStaminaPoolModal:378-395 = the ONE condition-color site not on the shared helpers (raw --dse-condition-color no SD-2 gate + raw classList.add(condition-effect-${effect}) = throw-on-whitespace footgun) → fold in applyConditionColor/applyConditionEffect (helpers already imported by 4 neighbors). + Minor: roleTint.ts .style write uncovered by styleGuard (code safe; add the scan + a roleOf unit suite). FIX PASS dispatched (Fable).
Minors → D3-KICKOFF FOLLOWUPS: kit-index tsColorFindings modern-color-fn asymmetry (styleGuardFindings already covers element sources); head:'' normalize→false in the kit; aria-selected on a role-less div (SR-acceptable); cardHead-contract test greps the sheet's first `.dse-head {` (positional).
*** 3 MAINTAINER DECISIONS — ALL DEFER, none block landing (Opus triage): T4 counter out-of-range clamp = decide FIRST (grazes explicit-state principle; clampInitial:false remedy); T3 stamina overshoot corridor (no corruption, cleaner, degenerate); T7 negotiation no-deselect (acceptable-per-design, no data risk). ***
*** VISUAL-QA before/with wt-finish = the plan's OWN acceptance gate (Scott reviews rendered output): feature cost-chip re-layout, statblock header re-layout, Skills/Stamina hover-eye→titled collapse headers, 44px bubbles/controls, Malice→horizontal stepper, temp-fill purple→deepskyblue. Spec-sanctioned but they MOVE the 'Legacy=today' line. ***
P09-REVIEW FIX PASS complete (commit 149cca1; Fable): MinionStaminaPoolModal.buildConditionIcons now uses applyConditionColor/applyConditionEffect (raw setProperty + classList.add REMOVED, hygiene-test-enforced); roleTint.ts now covered by styleGuardFindings (→[]) + a roleOf/applyRoleTint unit suite (test/dom/elements/role-tint.test.ts). Orch-verified: trailer clean, minion raw writes gone, tsc 0, 912/912 (+12), build clean, pushed origin. → the ONE Opus Should-Fix is CLOSED.
=== ✅✅✅✅ D2 PHASE (Plan 08 FOUNDATION + Plan 09 ELEMENT REDESIGN) COMPLETE, OPUS-APPROVED, & LANDABLE. Branch dse-framework @ 149cca1, pushed to origin. tsc 0 · 912/912 · build clean · all trailer-free · unlanded (nothing merged to main). AWAITING (all Scott's): visual-QA sign-off (the plan's OWN acceptance gate) + the 3 defer-able decisions + the land call (just wt-finish). NEXT PHASES: D3 (theme values / Steel overlay) + D4 (prefs). ===

=== SCOTT DECISIONS (2026-07-06): 
  • T4 counter out-of-range = FIX (show-as-stored): DONE (commit bfbdd07; Fable). kit `clampInitial?:boolean` opt (default clamps — regression-pinned; counter sets false). Out-of-range 25/max20 renders 25 (not 20); first − → 24 (stepClamp: ± guarded only by the far bound until in range); typed still clamps. VERIFIED vs legacy `git show afedb2a^:CounterView.ts` (exact arithmetic reproduced). Other steppers (stamina/malice/initiative) untouched. 926/926, tsc 0, build clean, pushed. → D2 FULLY CLOSED @ bfbdd07.
  • T3 stamina overshoot + T7 negotiation no-deselect = ACCEPT both (no code change; documented FOLLOWUPs — no data risk, cleaner/design-correct).
  • Visual-QA = Scott reviews the rendered v2 site HIMSELF before landing (I leave the branch as-is for his eyeball).
  • NEXT = START D3 (theming — the Steel [data-dse-theme="steel"] overlay on top of the Legacy base). D2 land call stays Scott's (post visual-QA). ===

################################################################
=== D3 (THEMING) STARTED. Plan: docs/superpowers/dse-overhaul/plans/2026-07-06-plan-10-d3-theming.md (7 tasks). Spec: D3-theming-print-spec.md. ===
Recon findings (the D2↔D3 reconciliation — the plan's biggest risk, front-loaded to Task 1):
  • D2 SHIPPED 67 token names (src/framework/tokens.ts DSE_TOKEN_NAMES) — DIFFERENT from the D3-spec's pre-D2 proposals. Map: hp-*→stamina-*; text-*→fg-*(+heading); damage→danger, healing→(none; heal reuses stamina-temp), warning→warn, selected→select, taken-turn→turn-done; ability-strike/ranged/…(8)→**act-main/maneuver/triggered/move/none/trait (D2's 6 Draw-Steel ACTION types — different taxonomy)**; tier-low/mid/high(+D2 crit); fx-*→metal-grad/bevel/emboss/rule(+rule-fade); surface-hover→hover; role-*(D2 has 12, NO role-malice; malice is standalone). D3 fills Steel VALUES for D2's ACTUAL names — does NOT re-narrow/rename.
  • theme.ts ALREADY has a working in-memory ThemeService (apply/onChange/cssVar/setActive) + DEFAULT_THEME_ID='steel' (= D3 OD-1 recommendation). PreferenceStore EXISTS (prefs.ts: interface + DsePreferenceStore + subscribe + reflect). NO Steel/Print value blocks yet (D2 deferred them here; base=Legacy, Opus-verified).
  • D3 = Task1 reconciliation map → Task2 ThemeService→PreferenceStore (theme pref descriptor, attr OMITTED = single-writer/D4 contract) → Task3 Steel value block (VISUAL — Scott QA; values from v2/docs/stylesheets/palette.css + DESIGN.md) → Task4 Steel-light → Task5 Print (@media print + [data-dse-print]) → Task6 build-guard + Legacy-fidelity assertion → Task7 D4-contract note.
  • OD defaults: proceeding on the D3-spec recommendations (steel default [already]; print=layer; Source Serif 4 fallback; "Match Obsidian (Legacy)"/"Steel" labels; manual palette sync; FULL light variant now) — flag to Scott, esp. OD-1 default + OD-4 fonts.
  • PLAN: execute T1 (reconciliation, analysis) + T2 (ThemeService, plumbing) autonomously; CHECK IN with Scott before/at T3 (the Steel LOOK — his design domain + visual QA).
BASE for D3 = bfbdd07 (D2 fully closed). Fable impl+review, Opus final.

D3 Task 1 (token reconciliation map + coverage-test scaffold): COMPLETE (commit 11f7e21; implementer=Fable; orch-verified: trailer clean, 64-row map, Legacy values spot-checked correct [stamina-healthy=limegreen, act-main=none, role-controller=var(--dse-fg-muted)], coverage test 2/2 green, 928/928, tsc 0, pushed). NOTE: union is **64 tokens** (not 67 — my miscount; pinned by tokens.test.ts). Map: workspace docs/superpowers/dse-overhaul/D3-token-map.md. act-* Steel proposal: main=strike-red, maneuver=purple, triggered=amber, move=ranged-blue, none=area-teal, trait=passive-grey (from --sc-ability-*, chained). Appendix B logs Legacy-vs-spec divergences (surface-sunken/border/hover/pad/font-display/stamina-temp/role-*/act-*). Doc/scaffold = no full review (real gates = coverage test + Scott's design review + Task-6 Legacy assertion). Map awaits SCOTT design review at the T3 gate.
  *** 5 OPEN DESIGN Qs FOR SCOTT (T3 gate): the act-* hue mapping; tier-crit/vp gold; Steel-only print scoping of role/act hues; + the D3 §8 ODs (default theme, fonts, light-variant). ***

D3 Task 2 (real ThemeService on PreferenceStore): COMPLETE (commit 9df54e2; implementer=Fable; orch-verified: trailer clean, prefs-backed active/setActive, attr-omitted descriptor + OD-5 ui, tsc 0, 938/938 [+10], pushed). NO full per-task review — plumbing seam w/ strong TDD (single plugin-owned subscribe / no-double-fire pinned / apply re-stamp + owner-cleanup / two-root popout / no document.body); folding the cross-cutting check into the D3 Opus final. Test-file churn (12 element + framework tests) = theme-service construction change rippling into setup (tsc 0 + green confirm). Concern→FOLLOWUPS: `void prefs.set` swallows a future storage rejection — add .catch when D4 wires saveData.
=== THEMING MACHINERY DONE (T1 map + T2 prefs-backed service). NEXT = T3 Steel value block = SCOTT DESIGN GATE. Then T4 Steel-light, T5 Print, T6 guard, T7 D4-note → D3 Opus final. BASE for T3 = 9df54e2. ===
D3 T3 DESIGN GATE (Scott, pending): (1) act-* Steel hues — PROPOSED main=strike-red/maneuver=purple/triggered=amber/move=ranged-blue/none=area-teal/trait=grey (from --sc-ability-*); (2) stamina-temp Steel = blue #5dade2 (continuity w/ today's deepskyblue) vs purple #7c5cd6 (DESIGN.md, avoids move-spine overlap); (3) tier-crit/vp gold #e3c14a; (4) ODs: default theme steel[recommended, already the code default] vs legacy; Steel fonts Source Serif 4 fallback[rec] vs Obsidian-only; full light variant now[rec]. Map: docs/superpowers/dse-overhaul/D3-token-map.md.

################################################################
=== ⛔ FABLE EXHAUSTED (out of usage CREDITS — not a resettable 5-hr limit; "/usage-credits or /model to switch"). 2026-07-06. ===
Scott's "last Fable session" call was right. Attempted to bank D4-D9 + F2 plans on 7 parallel Fable planners tonight — ALL 7 bounced "out of usage credits" → ZERO plans drafted (no files, no reports; a few read 12-13 tools before dying). 
=> REMAINING PLANNING IS NOW OPUS-ONLY (tomorrow): draft execution-ready plans for D4(preferences) / D5(rolling) / D6(compendium-ref) / D7(hero-suite) / D8(gm-subsystems) / D9(authoring) / F2(data-integration) — each: read its spec (docs/superpowers/dse-overhaul/D*-spec.md) + reconcile vs the BUILT state (F1 + D2 kit/tokens + D3 prefs/theme) + resolve ODs on recommendations. My dispatch prompts (with the built-state reconciliation context + F1-touch notes per spec) are in this session's transcript — reuse them. Deps: D6/D8 gated on F2 (cross-repo); D7 on D5/D6; D4 near-term (D3 consumes it).
=== SESSION-END STATE (2026-07-06): D2 COMPLETE+closed+pushed (Opus-approved, awaiting Scott's Obsidian visual-QA + land). D3: plan written (Plan 10) + machinery done (T1/T2 pushed @ 9df54e2); Steel authoring (T3-T5) paused for Scott's design gate + Obsidian visual-QA. D4-D9+F2: plans TODO (Opus). Branch dse-framework @ 9df54e2, pushed, unlanded. HANDOFF.md refreshed. ===
################################################################

################################################################
=== NEW DAY (2026-07-06 cont / next session) — OPUS-ONLY (Fable done). Scott: "keep moving forward... move to a good point where I can review everything." ===
GOAL: get the plugin FULLY THEMEABLE + BUILT so Scott can load it in Obsidian and review the whole D2+D3 result (Legacy fidelity + the Steel look). Proceeding on the design recommendations (the token map already resolved the hues); flag the taste calls for Scott to flip.
Subagents now = OPUS (implementer + review). Authoring D3 from the map (transcribe → CSS), not re-deciding.
D3 T3 (Steel value block, dark): COMPLETE (commit df7ad67; OPUS; orch-verified: trailer clean, Steel block present, pure additions [+311], Legacy base byte-unchanged, tsc 0, 947/947 [+9]). 59 tokens overridden (5 left-Legacy: page-bg/pad/touch-min/font-mono/rule-fade). Transcribed from D3-token-map.md. TASTE CALLS (one-line flips): stamina-temp=#5dade2 blue (alt purple #7c5cd6), tier-crit=#e3c14a gold, vp=#e3c14a gold. Included radius(0.4em)/crest-shape(shield polygon) per the map (DESIGN.md Steel uses them — good judgment). Report: .superpowers/sdd/task-3-report-d3.md (name -d3 to avoid clobbering the D2 stamina report).
D3 T4+T5+T6 (Steel-light + print/export + token-coverage guard): COMPLETE (commit 1966d30; OPUS; orch-verified: trailer clean, styles-source+3 theme-tests only, pure additions [637+/0−], Legacy base + Steel-dark byte-unchanged, tsc 0, 973/973 [+26]). Light=32 overrides (only-what-shifts). Print: hide .dse-btn/.dse-tabs__list/.dse-cond--add/.dse-cond-item__toggle/__cog+chevron; force-open .dse-collapse__region[hidden]; break-inside:avoid on [data-dse-element]/.dse-feature/.dse-pr/.dse-statgrid; print-color-adjust:exact on .dse-pr__badge/.dse-stamina__fill/__temp + Steel spines. Guard 64/64 (Legacy 64; Steel 59+5; Print 48+16) + phantom-token teeth + full Legacy-fidelity assertion. FIXED a real */-in-comment build bug. Report: task-456-report-d3.md.
  Flag (Scott): in a LEGACY print, tier/stamina/encounter colors DO colorize (only role/act spines are Steel-scoped) — map ambiguity; + rendered light-theme + Ctrl-P QA owed.
=== ✅ D3 COMPLETE (theme layer: Legacy base + Steel dark + Steel light + print; machinery T1 map + T2 prefs-service; guard 64/64). Values PROVISIONAL pending Scott's Obsidian visual-QA (taste calls flippable one-line each). Branch dse-framework @ 1966d30, pushed. Built plugin (main.js + styles.css) has data-dse-theme=steel ×8 + light + print — LOADABLE. NO D3 Opus final review yet (premature — Steel colors will move w/ Scott's QA). ===
=== ⇒ AT SCOTT'S REVIEW POINT (2026-07-06): D2 (all elements/modals on the kit) + D3 (full theming) done, built, pushed. Present: what changed / how to load in Obsidian / visual-QA checklist / taste calls to flip. ===
Added TEMP review commands (commit 4f429bc; OPUS): "Switch theme (Legacy⇄Steel)" (dse-cycle-theme — drives ThemeService.setActive, persists+live-reflow) + "Toggle print preview" (dse-toggle-print-preview — one-shot data-dse-print over rendered roots). D4 replaces w/ the real settings picker. 975/975.
=== 🚀 RELEASED 6.0.0-rc1 (2026-07-06, Scott-initiated via `devbox run release` — I ran the plugin `just release` recipe from the worktree after he confirmed by re-running). manifest+package→6.0.0-rc1; npm run build; version-bump commit d48dac1 on dse-framework (pushed); gh release create → https://github.com/SteelCompendium/draw-steel-elements/releases/tag/6.0.0-rc1 (main.js+manifest.json+styles.css attached; installable via BRAT). Scott saw the 3 heads-ups (temp commands ship, Steel colors provisional, released off the UNLANDED dse-framework branch) + proceeded. HEAD now d48dac1. ===
STILL PENDING (post-RC): Scott's visual-QA feedback (Steel color flips = 1-liners) → re-release/land as needed; D3 Opus final review (deferred while colors provisional); LAND D2+D3 to main (just wt-finish — the RC tag is on the feature branch, not main yet); D4-D9+F2 plans (Opus). NB: manifest/package now say 6.0.0-rc1 on the branch — bump to 6.0.0 (or next) at the final release.
################################################################
  Concerns (defer): (a) componentWrapper.ts NOT element-dead — negotiation(T7) still uses it → T10 gate waits for T7 (CORRECTS the T2-review note); (b) --dse-condition-color set WITHOUT SD-2 validation → Task 8 owns the validated helper (+ initiative/view.ts's remaining style.color sites); (c) pool-modal degenerate over-damage path differs (bounded stepper; persisted values unaffected).
################################################################
################################################################

################################################################
=== PLAN 11 (F4 VISUAL HARNESS) STARTED 2026-07-10. Plan: workspace docs/superpowers/dse-overhaul/plans/2026-07-10-plan-11-f4-visual-harness.md (6 tasks). Spec: F4-visual-harness-spec.md. Linear SC-9. BASE = c9e31f1. Orchestrator = Fable 5; final review = Opus (project convention). Plan 12 (real-Obsidian CDP camera) queued behind this. ===
P11 Task 1 (jest-free obsidian-core extraction): COMPLETE (commit 3b4cdd5; implementer=haiku, reviewer=sonnet; spec ✅ quality Approved; tsc 0, 976/976 identical, trailer clean [orch-verified]). NOTE: brief's literal `export *`-only rewrite was WRONG — tooltip.test.ts jest.spyOn(obsidian,'setTooltip') needs a local wrapper (ts-jest CJS __exportStar bindings are non-configurable); implementer caught it, reviewer confirmed necessary. → FOLLOWUPS (Minor, final-review triage): setIcon wrapper is symmetry-only (no test spies it) — keep or drop at final review.
P11 Task 2 (browser shim + devDeps): COMPLETE (commit 76492af; implementer=sonnet, reviewer=sonnet; spec ✅ quality Approved; shim byte-identical to brief, lucide@1.24.0 API matched [all 16 icon ids resolve, live-verified], deps dev-only, trailer clean, tsc 0 / 976 unchanged). NOTES: (a) implementer found+reverted an unexplained uncommitted ts-node devDep diff in the worktree pre-task — unknown origin, watch for recurrence; (b) npm install synced package-lock version field 5.1.1→6.0.0-rc1 (pre-existing drift from the RC bump, now fixed). → FOLLOWUPS (Minor, final-review triage): Notice.setMessage missing from core mock + shim (fails typecheck if future harness code calls it); `as never` double-cast in shim setIcon could be a typed IconNode cast.
P11 Task 3 (harness page: entry/vars/index/esbuild/feature fixture/fixtures-test): COMPLETE (commit 417b360; implementer=sonnet, reviewer=sonnet; spec ✅ quality Approved; TDD RED→GREEN, 979/979, tsc 0, harness.js+harness.css emitted [plugin styles ride main.ts's import graph], fixture byte-exact vs feature.test.ts FULL, vars.css coverage grep-verified complete, no shim/lucide/marked in jest graph, setActive confirmed sync+ordered, trailer clean). NOTE: reviewer's ts-node sandbox gap likely EXPLAINS the mystery reverted ts-node diff from Task 2 (someone added it to parse jest.config.ts outside devbox; devbox jest doesn't need it). → FOLLOWUPS (Minor, final-review triage): mountOne silently falls back unknown fixtureName→'default' in single-mount mode (surface an error instead once Task 4 adds named fixtures).
P11 Task 4 (all 11 fixtures + coverage gate): COMPLETE (commit 8e35ea4; implementer=sonnet, reviewer=sonnet; spec ✅ quality Approved; 13/13 fixture tests, 989/989 suite, tsc 0, build clean; 7 fixtures byte-identical to docs/ examples, 2 test-constant fallbacks [characteristics/values-row — no docs example exists, NOT doc rot], hr empty-by-design). Two reviewer-confirmed-faithful parity fixes in makeHarnessDeps(): CB-14 token-image seeding + FRAMEWORK_V2_DEPENDENCY_SCHEMAS registration (mirrors main.ts onload). ⚠️ REAL LATENT PLUGIN BUG surfaced: src/elements/initiative/view.ts:306 bare .then() no .catch() on resolveImageSourceOrDefault → unhandled rejection in a REAL vault when both primary+default token images are missing — file into FOLLOWUPS/polish (SC-4 family). → FOLLOWUPS (Minor): harness schema loop lacks main.ts's try/catch resilience wrapper.
P11 Task 5 (playwright camera + first sweep): COMPLETE (commits 2fc9142 + fix 3e628d6; implementer=sonnet, reviewer=sonnet, fix=sonnet; spec ✅ quality Approved after 1 fix round; 59/59 PNGs clean first attempt, chromium installed w/o sudo, narrowed runs verified, tsc 0 / 989). Fix round (2 Important plan-mandated + 1 Minor, orchestrator-adjudicated: spec's no-silent-failure goal governs over plan's literal code): sweep error boundary w/ curated failures + guaranteed browser.close [negative-path-tested], symmetric !print in --bg filter, empty-filter → exit 2. ORCHESTRATOR VISUAL VERIFICATION: Read feature--legacy-dark / statblock--{legacy,steel}-dark PNGs — real styled renders. 🎯 FIRST HARNESS FINDINGS (Steel-theme, → SC-10 backlog; re-verify in real Obsidian when Plan 12 lands): (1) Steel tier badges render as EMPTY colored pills — tier labels (≤11/12-16/17+) invisible (legacy shows them fine); (2) Steel act-spine overlaps/clips the first character of flush-left card text (legacy clean). → FOLLOWUPS (Minor): process.exit(2) validation paths bypass browser.close (pre-existing parity, low risk).
P11 Task 6 (shot-url + docs): COMPLETE (commits 2747fe5 + fix 86b8546; implementer=haiku, reviewer=sonnet, fix=sonnet; spec ✅ quality Approved after 1 fix round). Fix round: README hadn't been adjusted for the Task-5 fix behaviors (narrowing excludes print; bad flag → exit 2) — haiku's self-review FALSELY attested it had checked this (lesson: haiku self-attestations on cross-commit reconciliation are unreliable); + shot-url mkdirSync hardening. Final battery: tsc 0, jest 989, shots 59 clean, live v2 shot 1.2MB, trailers clean c9e31f1..HEAD.
=== ALL 6 P11 TASKS COMPLETE @ 86b8546 (pushed). NEXT: Opus whole-branch final review c9e31f1..86b8546, then post-plan doc sync + Plan 12 (CDP real-Obsidian camera) drafting. ===
=== ✅ PLAN 11 (F4 VISUAL HARNESS) COMPLETE 2026-07-10 @ b608541 (pushed, unlanded). 10 commits c9e31f1..b608541. Opus whole-branch final review: READY TO MERGE — Yes (no Criticals/Importants; all Minors triaged defer; 2 recommended hardenings APPLIED in b608541: README Steel-fallback-hex fidelity note + shim-not-CI note, mountFromParams error-seam jsdom test). Final state: tsc 0, 61 suites / 992 tests, `npm run shots` = 59 clean PNGs, shot-url live-site works. Key cross-task facts: Steel shots render the FALLBACK-HEX palette (no --sc-* snippet — the default-install look; by design, documented); jest gates the test-mock world, camera gates the shim world (re-run shots after shim edits). DEFERRED MINORS (triaged, none block): setIcon symmetry wrapper; shim Notice.setMessage + as-never cast; fixtureName→default fallback; schema-loop try/catch parity; exit(2) skips browser.close. REAL PLUGIN BUG for SC-4: initiative view.ts:306 unhandled rejection (missing token images, real vault). STEEL FINDINGS for SC-10: invisible tier-badge labels + act-spine text overlap (both steel-only, legacy clean; validate fixes against fallback hexes). NEXT: Plan 12 = F5 CDP real-Obsidian camera (spec to draft; Scott-approved direction). ===

################################################################
=== PLAN 12 (F5 OBSIDIAN CDP CAMERA) STARTED 2026-07-10. Plan: workspace docs/superpowers/dse-overhaul/plans/2026-07-10-plan-12-f5-obsidian-camera.md (5 tasks; Task 3 = spike). Spec: F5-obsidian-camera-spec.md. BASE = b608541. Decisions locked w/ Scott: second instance (own udd, port 9223, DISPLAY=:1), demo vault INTO repo (demo-vault/, plugin symlink, DS Compendium gitignored). NEVER touch Scott's running Obsidian. ===
P12 Task 1 (vault migration): COMPLETE (commit 4ef7a5b; implementer=haiku, reviewer=sonnet; spec ✅ quality Approved, reviewer re-ran gates independently: tsc 0, 992/992; exactly 15 tracked files [brief's ~12 was an estimate; authoritative list sums to 15], symlink mode-120000 ../../.., DS Compendium/workspace.json/BRAT ignored, source vault untouched [10 top-level entries]). ⚠️ ts-node package.json churn RECURRED during gate runs (3rd sighting; implementer reverted) — something in `npx jest` under devbox adds ts-node to package.json. FOLLOWUP: either add ts-node as a real devDependency deliberately or find the auto-installer; watch every task.
P12 Task 2 (aliases gate + notes-gen): COMPLETE (commit f1cb777; implementer=haiku, reviewer=sonnet; spec ✅ quality Approved; all 11 alias pairs verified against definition files, empty-body hr fence traced byte-exact, Harness/ untracked, no ts-node churn, 993/993, tsc 0).
P12 Task 3 (SPIKE — launch/attach/one shot): COMPLETE (commit a77a4a1; implementer=fable, reviewer=sonnet; Approved-with-notes-for-Task-4). MECHANICS PROVEN (safety airtight, code-verified): raw CDP over WebSocket (Playwright connectOverCDP INCOMPATIBLE w/ Obsidian Electron — /json/list → Runtime.evaluate + Page.captureScreenshot); /usr/bin/obsidian = old installer, self-updates asar into udd → WARM-UP LAUNCH required; --window-size ignored → geometry via <udd>/<vaultId>.json; obsidian.json seed skips picker, NO trust modal; setEnable(true)+enablePluginAndSave works; app.changeTheme('moonstone'|'obsidian') = chrome dark/light; quit = electron.remote.app.quit() (no app:quit cmd); tall elements = Emulation.setDeviceMetricsOverride; strip notices pre-shot. ⚠️ IMPORTANT (fold into Task 4): spike PNG statblock--obsidian-legacy-dark.png is MISLABELED — actually STEEL (plugin default; spike never setActive; orchestrator caught visually, reviewer traced) — Task 4 must drive frameworkV2.services.theme.setActive per shot as a distinct axis + regenerate/replace the mislabeled file; also drop dead --spike flag, loosen the before==='dark' assert. 🎯 ACCIDENTAL EARLY ADJUDICATION (ground truth, steel-dark): BOTH F4 Steel findings CONFIRMED in real Obsidian — invisible tier-badge labels + act-spine text overlap. F4 fidelity validated. Fidelity delta noted: real Obsidian renders SERIF body font (F4 vars.css uses sans) — reconcile in Task 5 docs.
P12 Task 4 (full 44-shot camera): COMPLETE (commit df5b1e0; implementer=fable, reviewer=sonnet; spec ✅ quality Approved; 44/44 clean in 57.9s warm, theme axis verified end-to-end vs framework source [element-root data-dse-theme wait], mislabeled spike PNG replaced w/ byte proof [legacy 574,268B vs steel 609,557B], flags/error/teardown match F4 contract, vault-churn policy = committed stable 1.12.7 migration baselines w/ 4-run byte-identical evidence + camera restores steel/dark before quit]. Fixed en route: spike's height>20 render gate broke horizontal-rule (1020×14) → width>0&&height>0; that failure exercised the full --ERROR/continue/exit-1 contract for real. FOOTGUN logged: devbox run -- bash -c EATS $? — check exit codes from the outer shell. → FOLLOWUPS (Minor, final-review triage): hotkeys.json {} maybe .gitignore instead of committed; setActive persist is fire-and-forget before quit (pre-existing void prefs.set). ORCHESTRATOR VISUAL CHECK: corrected legacy-dark statblock = genuinely flat legacy skin ✓; fidelity delta = real Obsidian renders serif body font vs F4 sans (document in T5). ADJUDICATION (from spike steel shot + F4 pairs): BOTH Steel findings CONFIRMED in ground truth.
P12 Task 5 (docs + final battery): COMPLETE (commit ace65d1; implementer=sonnet, reviewer=sonnet; spec ✅ quality Approved; every README claim independently re-verified against camera source [dozen citations spot-checked accurate]; implementer correctly attributed the serif-font claim to orchestrator adjudication instead of false self-attestation — the P11-T6 failure mode explicitly avoided). Battery: tsc 0, 993/993, shots 59 clean, obsidian-shots 44 clean, trailers empty.
=== ALL 5 P12 TASKS COMPLETE @ ace65d1 (pushed). NEXT: Opus whole-branch final review b608541..ace65d1. ===
=== ✅ PLAN 12 (F5 OBSIDIAN CDP CAMERA) COMPLETE 2026-07-10 @ ace65d1 (pushed, unlanded). 5 commits b608541..ace65d1. Opus whole-branch final review: READY TO MERGE — Yes (zero fix-now; safety layered [port-check/udd-guard/own-child-kill/scratch-udd]; clean tree after runs = restore-path proof; both theme axes race-scoped). Final state: tsc 0, 993 tests, shots 59 clean, obsidian-shots 44 clean ~58s. GROUND TRUTH: both F4 Steel findings CONFIRMED in real Obsidian (invisible tier labels + spine clipping → SC-10); F4 fidelity validated (deltas documented: serif vs sans, base-CSS chips, shared fallback-hex Steel). DEFERRED (triaged): hotkeys.json {} commit; fire-and-forget persist pre-quit; ts-node devDep hygiene (SEPARATE commit, cross-plan — 4th sighting pattern: npx jest under devbox adds ts-node to package.json); tracked .obsidian churn on future Obsidian self-updates (signal: dirty tree after obsidian-shots → widen gitignore); spec as-built deltas FIXED in workspace (port 9223, raw CDP, warm-up). POST-PLAN GATE (Scott): open demo-vault/ in his own Obsidian once, confirm demo setup works; then retire ~/Documents/draw-steel-elements-demo at leisure. ===
